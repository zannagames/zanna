//===----------------------------------------------------------------------===//
//
// Part of the Zanna project, under the GNU GPL v3.
// See LICENSE for license information.
//
//===----------------------------------------------------------------------===//
//
// File: zannastudio/src/ui/scene_editor_3d_contract_03.zia
// Purpose: Virtual method contract segment 3 for composable 3D scene-editor implementation layers.
// Key invariants:
//   - Signatures mirror the public/private controller surface and provide neutral dispatch fallbacks.
// Ownership/Lifetime:
//   - Contract instances are implementation scaffolding and own no external resources.
// Links: zannastudio/src/ui/scene_editor_3d.zia
//
//===----------------------------------------------------------------------===//

module scene_editor_3d_contract_03;

bind "../core/document";
bind "../core/project_manager";
bind "../services/file_utils";
bind "../services/material_library_3d";
bind "../services/project_locator";
bind "../services/safe_io";
bind "scene_asset_browser";
bind "scene_bake_3d";
bind "scene_camera_3d";
bind "scene_camera_inspector_3d";
bind "scene_clipboard";
bind "scene_collider_3d";
bind "scene_collider_inspector_3d";
bind "scene_component_authoring_controller";
bind "scene_component_palette";
bind "scene_component_schema";
bind "scene_hierarchy_search";
bind "scene_hierarchy_3d";
bind "scene_history";
bind "scene_selection";
bind "scene_light_inspector_3d";
bind "scene_light_3d";
bind "scene_material_inspector_3d";
bind "scene_material_3d";
bind "scene_metadata_inspector_3d";
bind "scene_pane_docking";
bind "scene_terrain_3d";
bind "scene_terrain_inspector_3d";
bind "scene_transform_3d";
bind Zanna.GUI as GUI;
bind SceneGraph = Zanna.Graphics3D.SceneGraph;
bind SceneNode = Zanna.Graphics3D.SceneNode;
bind SceneAsset = Zanna.Graphics3D.SceneAsset;
bind Mesh3D = Zanna.Graphics3D.Mesh3D;
bind Material3D = Zanna.Graphics3D.Material3D;
bind Light3D = Zanna.Graphics3D.Light3D;
bind Canvas3D = Zanna.Graphics3D.Canvas3D;
bind RenderTarget3D = Zanna.Graphics3D.RenderTarget3D;
bind Camera3D = Zanna.Graphics3D.Camera3D;
bind PostFX3D = Zanna.Graphics3D.PostFX3D;
bind LightBaker3D = Zanna.Graphics3D.LightBaker3D;
bind LightProbeGrid3D = Zanna.Graphics3D.LightProbeGrid3D;
bind CubeMap3D = Zanna.Graphics3D.CubeMap3D;
bind Water3D = Zanna.Graphics3D.Water3D;
bind Mesh3D2 = Zanna.Graphics3D.Mesh3D;
bind Pixels = Zanna.Graphics.Pixels;
bind Color = Zanna.Graphics.Color;
bind Vec3 = Zanna.Math.Vec3;
bind Quat = Zanna.Math.Quat;
bind Mat4 = Zanna.Math.Mat4;
bind RayHit3D = Zanna.Graphics3D.RayHit3D;
bind Seq = Zanna.Collections.Seq;
bind Math = Zanna.Math;
bind Convert = Zanna.Core.Convert;
bind Input = Zanna.Input;
bind File = Zanna.IO.File;
bind Path = Zanna.IO.Path;
bind Str = Zanna.String;
bind Parse = Zanna.Core.Parse;
bind "scene_editor_3d_model" as SceneEditor3DModel;
bind "scene_editor_3d_contract_02" as PreviousContract;

/// @brief Dispatch declarations used by focused SceneEditor3D layers.
class SceneEditor3DContract03 extends PreviousContract.SceneEditor3DContract02 {
    /// @brief Neutral dispatch slot for AbsNumber3D.
    hide func AbsNumber3D(_value: Number) -> Number { return 0.0; }

    /// @brief Neutral dispatch slot for PackPreviewColor.
    hide func PackPreviewColor(_red: Number,
                               _green: Number,
                               _blue: Number) -> Integer { return 0; }

    /// @brief Neutral dispatch slot for ApplyProjectPreviewEnvironment.
    hide func ApplyProjectPreviewEnvironment(
        _canvas: Canvas3D,
        _state: SceneEditor3DModel.ScenePreview3DRenderState,
        _authoredSkyPresent: Boolean,
        _assist: Boolean) {}

    /// @brief Neutral dispatch slot for PumpMaterialLibraryControls.
    hide func PumpMaterialLibraryControls() {}

    /// @brief Neutral dispatch slot for ReloadMaterialLibrary.
    expose func ReloadMaterialLibrary() {}

    /// @brief Neutral dispatch slot for UpdateMaterialLibraryControls.
    hide func UpdateMaterialLibraryControls() {}

    /// @brief Neutral dispatch slot for SaveSelectedMaterialToLibrary.
    expose func SaveSelectedMaterialToLibrary() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for SyncHistoryLabels.
    hide func SyncHistoryLabels() {}

    /// @brief Neutral dispatch slot for RefreshHistoryPanel.
    expose func RefreshHistoryPanel() {}

    /// @brief Neutral dispatch slot for JumpToHistoryRow.
    expose func JumpToHistoryRow(_row: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for PumpHistoryPanel.
    hide func PumpHistoryPanel() {}

    /// @brief Neutral dispatch slot for RefreshMaterialSwatch.
    expose func RefreshMaterialSwatch() {}

    /// @brief Neutral dispatch slot for ApplyLibraryMaterialToSelection.
    expose func ApplyLibraryMaterialToSelection() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ActiveLookThroughNode.
    hide func ActiveLookThroughNode() -> SceneNode { return null; }

    /// @brief Neutral dispatch slot for NormalizedVectorOr.
    hide func NormalizedVectorOr(_vector: Vec3,
                                 _fallbackX: Number,
                                 _fallbackY: Number,
                                 _fallbackZ: Number) -> Vec3 { return null; }

    /// @brief Neutral dispatch slot for ApplyAuthoredCameraPose.
    hide func ApplyAuthoredCameraPose(_target: Camera3D,
                                      _node: SceneNode) {}

    /// @brief Neutral dispatch slot for ApplyNodeMaterial.
    expose func ApplyNodeMaterial(_nodeIndex: Integer,
                                  _red: Number,
                                  _green: Number,
                                  _blue: Number,
                                  _alpha: Number,
                                  _metallic: Number,
                                  _roughness: Number,
                                  _ambientOcclusion: Number,
                                  _alphaMode: Integer,
                                  _doubleSided: Boolean,
                                  _unlit: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplySelectedMaterialPatch.
    expose func ApplySelectedMaterialPatch(
        _patch: scene_material_3d.MaterialPatch3D) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for RemoveNodeMaterial.
    expose func RemoveNodeMaterial(_nodeIndex: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for RemoveSelectedMaterials.
    expose func RemoveSelectedMaterials() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplyMaterialMapPath.
    expose func ApplyMaterialMapPath(_nodeIndex: Integer,
                                     _slot: Integer,
                                     _path: String) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplySelectedMaterialMapPath.
    expose func ApplySelectedMaterialMapPath(_slot: Integer,
                                             _path: String) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for LoadMaterialMapPath.
    hide func LoadMaterialMapPath(_path: String) -> Any { return null; }

    /// @brief Neutral dispatch slot for ClearMaterialMap.
    expose func ClearMaterialMap(_nodeIndex: Integer,
                                 _slot: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ClearSelectedMaterialMap.
    expose func ClearSelectedMaterialMap(_slot: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ImportAssetPath.
    expose func ImportAssetPath(_path: String) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ImportAssetPathAt.
    expose func ImportAssetPathAt(_path: String,
                                  _useSpawn: Boolean,
                                  _spawnX: Number,
                                  _spawnY: Number,
                                  _spawnZ: Number) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AddSnapSpinner.
    expose func AddSnapSpinner(_minimum: Number,
                             _maximum: Number,
                             _step: Number,
                             _value: Number,
                             _accessibleName: String) -> GUI.Spinner { return null; }

    /// @brief Neutral dispatch slot for AddTransformSpinner.
    expose func AddTransformSpinner(_parent: GUI.HBox,
                                  _label: String,
                                  _value: Number) -> GUI.Spinner { return null; }

    /// @brief Neutral dispatch slot for AddRotationSpinner.
    expose func AddRotationSpinner(_parent: GUI.HBox,
                                 _label: String) -> GUI.Spinner { return null; }

    /// @brief Neutral dispatch slot for LoadGraphFromContent.
    hide func LoadGraphFromContent(_content: String) -> SceneGraph { return null; }

    /// @brief Neutral dispatch slot for ImportAsset.
    hide func ImportAsset() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for CommitScene.
    hide func CommitScene(_message: String) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for PushHistory.
    hide func PushHistory(_stack: List[String], _content: String) {}

    /// @brief Neutral dispatch slot for RollBackFailedCommit.
    hide func RollBackFailedCommit(_message: String) {}

    /// @brief Neutral dispatch slot for AddTerrainFromMenu.
    expose func AddTerrainFromMenu() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AddTerrainAtPointer.
    expose func AddTerrainAtPointer(_physicalX: Number,
                                    _physicalY: Number) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AddTerrainAt.
    hide func AddTerrainAt(_spawnX: Number,
                           _spawnY: Number,
                           _spawnZ: Number,
                           _armRename: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for TerrainHeightsDiffer.
    hide func TerrainHeightsDiffer(_left: List[Number],
                                   _right: List[Number]) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for CommitTerrainMesh.
    hide func CommitTerrainMesh(_node: SceneNode,
                                _heights: List[Number],
                                _columns: Integer,
                                _rows: Integer,
                                _spacing: Number,
                                _message: String) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for RegenerateSelectedTerrain.
    expose func RegenerateSelectedTerrain() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for FlattenSelectedTerrain.
    expose func FlattenSelectedTerrain() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for SculptSelectedTerrainAt.
    expose func SculptSelectedTerrainAt(
        _localX: Number,
        _localZ: Number,
        _radius: Number,
        _strength: Number,
        _mode: Integer,
        _flattenHeight: Number) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AddPrimitive.
    expose func AddPrimitive(_kind: Integer, _asChild: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AddPrimitiveFromMenu.
    expose func AddPrimitiveFromMenu(_kind: Integer,
                                     _asChild: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AddPrimitiveAtPointer.
    expose func AddPrimitiveAtPointer(_kind: Integer,
                                      _physicalX: Number,
                                      _physicalY: Number) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ViewportSpawnPoint.
    expose func ViewportSpawnPoint(_viewportX: Integer,
                                   _viewportY: Integer) -> Vec3 { return null; }

    /// @brief Neutral dispatch slot for AddPrimitiveAt.
    hide func AddPrimitiveAt(_kind: Integer,
                             _asChild: Boolean,
                             _spawnX: Number,
                             _spawnY: Number,
                             _spawnZ: Number,
                             _armRename: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for FindNodeIndex.
    hide func FindNodeIndex(_nodes: List[SceneNode],
                            _needle: SceneNode) -> Integer { return 0; }

    /// @brief Neutral dispatch slot for TopLevelIndicesFor.
    hide func TopLevelIndicesFor(_nodes: List[SceneNode],
                                 _indices: List[Integer],
                                 _primary: Integer) -> List[Integer] { return []; }

    /// @brief Neutral dispatch slot for TopLevelSelectedIndices.
    hide func TopLevelSelectedIndices() -> List[Integer] { return []; }

    /// @brief Neutral dispatch slot for TopLevelSelectedNodes.
    hide func TopLevelSelectedNodes() -> List[SceneNode] { return []; }

    /// @brief Neutral dispatch slot for NodeWorldBounds.
    hide func NodeWorldBounds(_node: SceneNode) -> SceneEditor3DModel.WorldBounds3D { return null; }

    /// @brief Neutral dispatch slot for MergeWorldBounds.
    hide func MergeWorldBounds(_target: SceneEditor3DModel.WorldBounds3D,
                               _source: SceneEditor3DModel.WorldBounds3D) {}

    /// @brief Neutral dispatch slot for NodeWithinRoots.
    hide func NodeWithinRoots(_node: SceneNode,
                              _roots: List[SceneNode]) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for SurfaceRootWorldBounds.
    hide func SurfaceRootWorldBounds(_root: SceneNode) -> SceneEditor3DModel.WorldBounds3D { return null; }

    /// @brief Neutral dispatch slot for SuppressSelectedDropMeshes.
    hide func SuppressSelectedDropMeshes(
        _roots: List[SceneNode],
        _suppressedNodes: List[SceneNode],
        _suppressedMeshes: List[Mesh3D]) {}

    /// @brief Neutral dispatch slot for RestoreSuppressedDropMeshes.
    hide func RestoreSuppressedDropMeshes(
        _nodes: List[SceneNode],
        _meshes: List[Mesh3D]) {}

    /// @brief Neutral dispatch slot for NearestDropSurface.
    hide func NearestDropSurface(_origin: Vec3) -> RayHit3D { return null; }

    /// @brief Neutral dispatch slot for QuerySurfacePlacements.
    hide func QuerySurfacePlacements(
        _roots: List[SceneNode],
        _startClearance: Number) -> List[SceneEditor3DModel.SurfacePlacement3D] { return []; }

    /// @brief Neutral dispatch slot for UpwardSurfaceNormal.
    hide func UpwardSurfaceNormal(_normal: Vec3) -> Vec3 { return null; }

    /// @brief Neutral dispatch slot for SurfaceAlignmentDelta.
    hide func SurfaceAlignmentDelta(
        _root: SceneNode,
        _normal: Vec3) -> Quat { return null; }

    /// @brief Neutral dispatch slot for SurfaceAlignedWorldMatrix.
    hide func SurfaceAlignedWorldMatrix(
        _root: SceneNode,
        _startWorld: Mat4,
        _normal: Vec3) -> Mat4 { return null; }

    /// @brief Neutral dispatch slot for ApplyRootsToNearestSurfaces.
    hide func ApplyRootsToNearestSurfaces(
        _rootIndices: List[Integer],
        _roots: List[SceneNode],
        _alignNormals: Boolean,
        _startClearance: Number) -> Integer { return 0; }

    /// @brief Neutral dispatch slot for DropSelectedToSurface.
    expose func DropSelectedToSurface() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for SubtreeRelativeDepth.
    hide func SubtreeRelativeDepth(_node: SceneNode) -> Integer { return 0; }

    /// @brief Neutral dispatch slot for ParentTargetFits.
    hide func ParentTargetFits(_targetIndex: Integer,
                               _roots: List[SceneNode]) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ReparentSelectedNodes.
    expose func ReparentSelectedNodes(_targetIndex: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplyHierarchyDrop.
    expose func ApplyHierarchyDrop(_sourceIndex: Integer,
                                   _targetIndex: Integer,
                                   _position: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ChildOrderMatches.
    hide func ChildOrderMatches(_parent: SceneNode,
                                _desired: List[SceneNode]) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for MoveSelectedBeside.
    hide func MoveSelectedBeside(_targetIndex: Integer,
                                 _after: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for SelectedSiblingBlock.
    hide func SelectedSiblingBlock() -> List[SceneNode] { return []; }

    /// @brief Neutral dispatch slot for DirectChildIndex.
    hide func DirectChildIndex(_parent: SceneNode,
                               _child: SceneNode) -> Integer { return 0; }

    /// @brief Neutral dispatch slot for MoveSelectedSiblings.
    expose func MoveSelectedSiblings(_direction: Integer) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for DeleteSelectedNodes.
    expose func DeleteSelectedNodes() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for FlattenGraph.
    hide func FlattenGraph(_graph: SceneGraph) -> List[SceneNode] { return []; }

    /// @brief Neutral dispatch slot for AppendFlattenedClone.
    hide func AppendFlattenedClone(_node: SceneNode,
                                   _nodes: List[SceneNode]) {}

    /// @brief Neutral dispatch slot for UniqueDuplicateNodeName.
    hide func UniqueDuplicateNodeName(_source: SceneNode) -> String { return ""; }

    /// @brief Neutral dispatch slot for RollBackGroupEdit.
    hide func RollBackGroupEdit(_previousSelection: List[Integer],
                                _previousPrimary: Integer,
                                _message: String) {}

    /// @brief Neutral dispatch slot for DuplicateSelectedNodes.
    expose func DuplicateSelectedNodes() -> Integer { return 0; }

    /// @brief Neutral dispatch slot for ApplySelectedNode.
    hide func ApplySelectedNode() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplySelectedStatic.
    expose func ApplySelectedStatic(_nextStatic: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplySelectedVisibility.
    expose func ApplySelectedVisibility(_nextVisible: Boolean) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ApplySelectedMaterialFromControls.
    expose func ApplySelectedMaterialFromControls() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for ChooseMaterialMap.
    hide func ChooseMaterialMap() -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AssignSelectedMaterialMap.
    expose func AssignSelectedMaterialMap(_slot: Integer,
                                          _texture: Any) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for AssignMaterialMap.
    hide func AssignMaterialMap(_nodeIndex: Integer,
                                _slot: Integer,
                                _texture: Any) -> Boolean { return false; }

    /// @brief Neutral dispatch slot for SetEditorControlsEnabled.
    expose func SetEditorControlsEnabled(_enabled: Boolean) {}

    /// @brief Neutral dispatch slot for CanShowSelectedNodes.
    expose func CanShowSelectedNodes() -> Boolean { return false; }

}
