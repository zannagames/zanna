//===----------------------------------------------------------------------===//
//
// Part of the Zanna project, under the GNU GPL v3.
// See LICENSE for license information.
//
//===----------------------------------------------------------------------===//
//
// File: zannastudio/src/commands/command_catalog.zia
// Purpose: Declare Studio commands and platform-native default shortcuts.
// Key invariants:
//   - One catalog supplies menu, palette, toolbar, and global-shortcut metadata.
//   - Canonical Ctrl defaults materialize as Command on macOS.
//   - Explicit user overrides are preserved verbatim by CommandRegistry.
// Ownership/Lifetime: Returns fresh command specifications to each registry.
// Links: zannastudio/src/commands/command_registry.zia,
//        zannastudio/src/ui/menu_chrome.zia
//
//===----------------------------------------------------------------------===//

module command_catalog;

bind Str = Zanna.String;
bind Machine = Zanna.System.Machine;

// ============================================================================
// MODULE: command_catalog
// PURPOSE: Declarative command metadata for Zanna Studio. This module owns the
//          static list of command ids, labels, shortcuts, capabilities, and
//          palette visibility so CommandRegistry can focus on runtime behavior.
//
// WHERE-THIS-FITS: commands/command_registry.zia imports this file during
//          startup, converts each CommandSpec into a CommandEntry, and then
//          applies language-service capability checks and shortcut wiring.
//
// TEACHING-NOTE: Add new commands here first. Keeping command declarations in
//          one small data module makes it easy to review command ids and avoids
//          burying metadata inside palette or shortcut implementation code.
// ============================================================================

/// @brief Static metadata for a command known to Zanna Studio.
/// @details CommandSpec deliberately contains no runtime state. The registry
///          copies these fields into CommandEntry objects and applies runtime
///          language-service availability checks there. Category is stored here
///          because it is command metadata, not registry behavior.
class CommandSpec {
    expose String id;
    expose String label;
    expose String description;
    expose String category;
    expose String shortcut;
    expose String capability;
    expose Boolean showInPalette;

    /// @brief Initialize an empty command specification.
    /// @details Fields are populated by AddSpec so each catalog entry can be
    ///          read as a compact one-line declaration.
    expose func init() {
        id = "";
        label = "";
        description = "";
        category = "";
        shortcut = "";
        capability = "";
        showInPalette = true;
    }
}

/// @brief Return the full built-in command catalog in registration order.
/// @details Registration order is user visible in a few places: shortcut
///          conflict reporting, command palette ordering, and probe output.
///          Preserve the existing order unless there is a deliberate UI reason
///          to regroup commands.
expose func DefaultCommandSpecs() -> List[CommandSpec] {
    var specs: List[CommandSpec] = [];
    AddSpec(specs, "new", "New File", "Create a new untitled file", "Ctrl+N", "", true);
    AddSpec(specs, "packageproject", "Package Project", "Run zanna package on the open project as a streamed job", "", "", true);
    AddSpec(specs, "newproject2dgame", "New Project: 2D Game", "Scaffold a scene-driven 2D game playable through Run Scene", "", "", true);
    AddSpec(specs, "newproject3dgame", "New Project: 3D Game", "Scaffold a native scene-driven 3D game playable through Play", "", "", true);
    AddSpec(specs, "newscene2d", "New 2D Scene", "Create an editable tile and object scene", "", "", true);
    AddSpec(specs, "newscene3d", "New 3D Scene", "Create an editable VSCN hierarchy", "", "", true);
    AddSpec(specs, "open", "Open File...", "Open a file from disk", "Ctrl+O", "", true);
    AddSpec(specs, "openfolder", "Open Folder...", "Open a project folder", "Ctrl+Alt+O", "", true);
    AddSpec(specs, "addfolder", "Add Folder to Workspace...", "Add another folder as a workspace root", "", "", true);
    AddSpec(specs, "removefolder", "Remove Folder from Workspace", "Detach the selected folder without deleting its files", "", "", true);
    AddSpec(specs, "closeworkspace", "Close Workspace", "Detach every open project folder while keeping editor tabs open", "", "", true);
    AddSpec(specs, "comparewithsaved", "Compare with Saved", "Side-by-side diff of the active document against its saved file", "", "", true);
    AddSpec(specs, "newprojectconsole", "New Project: Console App", "Scaffold a console application and open it", "", "", true);
    AddSpec(specs, "newprojectgui", "New Project: GUI App", "Scaffold a windowed application and open it", "", "", true);
    AddSpec(specs, "newprojectlibrary", "New Project: Library", "Scaffold a reusable library and open it", "", "", true);
    AddSpec(specs, "palette", "Command Palette", "Open the command palette", "Ctrl+Shift+P", "", false);
    AddSpec(specs, "terminal", "Toggle Terminal", "Show or hide the integrated terminal", "Ctrl+`", "", true);
    AddSpec(specs, "terminalnew", "Terminal: New", "Create and select a bounded terminal session", "", "", true);
    AddSpec(specs, "terminalnext", "Terminal: Select Next", "Select the next terminal session", "", "", true);
    AddSpec(specs, "terminalrename", "Terminal: Rename", "Rename the active terminal session", "", "", true);
    AddSpec(specs, "terminalclose", "Terminal: Close", "Close the active terminal session", "", "", true);
    AddSpec(specs, "save", "Save", "Save the current file", "Ctrl+S", "", true);
    AddSpec(specs, "saveas", "Save As...", "Save the current file under a new path", "Ctrl+Shift+S", "", true);
    AddSpec(specs, "saveall", "Save All", "Save all open files", "Ctrl+Alt+Shift+S", "", true);
    AddSpec(specs, "close", "Close File", "Close the current tab", "Ctrl+W", "", true);
    AddSpec(specs, "reload", "Reload File", "Reload file from disk", "Ctrl+Shift+R", "", true);
    AddSpec(specs, "recentfiles", "Recent Files", "Show recently opened files", "Ctrl+K Ctrl+R", "", true);
    AddSpec(specs, "reopenclosed", "Reopen Closed File", "Reopen the most recently closed named file", "Ctrl+Alt+T", "", true);
    AddSpec(specs, "find", "Find / Replace", "Open the find bar", "Ctrl+F", "", true);
    AddSpec(specs, "goto", "Go To Line", "Jump to a specific line number", "Ctrl+G", "", true);
    AddSpec(specs, "build", "Build", "Compile the current file or project", "Ctrl+Shift+B", "", true);
    AddSpec(specs, "buildrun", "Build and Run", "Build and run the current file or project", "F5", "", true);
    AddSpec(specs, "run", "Run", "Run the current file or project through the Zanna toolchain", "Ctrl+F5", "", true);
    AddSpec(specs, "runscene", "Run Scene", "Run the owning project with the active saved scene path appended", "Ctrl+R", "", true);
    AddSpec(specs, "stopjob", "Stop Running Job", "Terminate the active build or run process", "Shift+F5", "", true);
    AddSpec(specs, "debugstart", "Start Debugging", "Launch the active file in the debug protocol", "F6", "", true);
    AddSpec(specs, "debugcontinue", "Continue Debugging", "Continue a paused debug session", "F7", "", true);
    AddSpec(specs, "debugstepover", "Step Over", "Step over the current debug line", "F10", "", true);
    AddSpec(specs, "debugstepin", "Step Into", "Step into the next debuggable call", "Ctrl+F10", "", true);
    AddSpec(specs, "debugstepout", "Step Out", "Run until the current function returns", "Ctrl+Shift+F10", "", true);
    AddSpec(specs, "debugpause", "Pause Debugging", "Pause the active debug session", "", "", true);
    AddSpec(specs, "debugrestart", "Restart Debugging", "Terminate and relaunch the active debug session", "Ctrl+Shift+F5", "", true);
    AddSpec(specs, "debugstop", "Stop Debugging", "Terminate the active debug session", "", "", true);
    AddSpec(specs, "debugruntocursor", "Run to Cursor", "Continue until execution reaches the cursor line", "Ctrl+F8", "", true);
    AddSpec(specs, "debugaddwatch", "Add Watch", "Add a debugger watch expression", "", "", true);
    AddSpec(specs, "debugremovewatch", "Remove Selected Watch", "Remove the selected debugger watch expression", "", "", true);
    AddSpec(specs, "debugrefreshwatches", "Refresh Watches", "Re-evaluate all debugger watch expressions", "", "", true);
    AddSpec(specs, "debugclearwatches", "Clear Watches", "Remove all debugger watch expressions", "", "", true);
    AddSpec(specs, "togglebreakpoint", "Toggle Breakpoint", "Set or clear a breakpoint at the cursor", "F9", "", true);
    AddSpec(specs, "debugcondition", "Conditional Breakpoint…", "Set a breakpoint that stops only when an expression is true", "", "", true);
    AddSpec(specs, "debuglogpoint", "Add Logpoint…", "Log a message (with {expr}) at a line without stopping", "", "", true);
    AddSpec(specs, "togglesidebar", "Toggle Primary Side Bar", "Show or hide Explorer, Source Control, or Run and Debug", "Ctrl+B", "", true);
    AddSpec(specs, "movesidebarleft", "Move Primary Side Bar Left", "Move Explorer, Source Control, and Run and Debug to the left edge", "", "", true);
    AddSpec(specs, "movesidebarright", "Move Primary Side Bar Right", "Move Explorer, Source Control, and Run and Debug to the right edge", "", "", true);
    AddSpec(specs, "dockpanelsleft", "Dock Panel Group Left", "Move and merge the primary tool-panel group at the left edge", "", "", true);
    AddSpec(specs, "dockpanelsbottom", "Dock Panel Group Bottom", "Move and merge the primary tool-panel group below the editor", "", "", true);
    AddSpec(specs, "dockpanelsright", "Dock Panel Group Right", "Move and merge the primary tool-panel group at the right edge", "", "", true);
    AddSpec(specs, "floatpanelgroup", "Float Primary Panel Group", "Move the complete primary tool-panel group into a movable window", "", "", true);
    AddSpec(specs, "movetoolleft", "Move Active Tool Left", "Split the selected tool into the left dock group", "", "", true);
    AddSpec(specs, "movetoolbottom", "Move Active Tool to Bottom", "Split the selected tool into the bottom dock group", "", "", true);
    AddSpec(specs, "movetoolright", "Move Active Tool Right", "Split the selected tool into the right dock group", "", "", true);
    AddSpec(specs, "movetoolfloating", "Move Active Tool to Floating Window", "Split the selected tool into the movable floating group", "", "", true);
    AddSpec(specs, "scenefocus", "Toggle Scene Focus", "Maximize the active 2D or 3D scene while preserving the exact scene and workbench layout", "Shift+Space", "", true);
    AddSpec(specs, "resetworkspacelayout", "Reset Workspace Layout", "Restore default sidebar, panel docking, tab order, and editor split", "", "", true);
    AddSpec(specs, "spliteditorright", "Split Editor Right", "Open another view of the active document to the right", "Ctrl+\\", "", true);
    AddSpec(specs, "focusotherpane", "Focus Other Editor Pane", "Move focus to the other split pane", "", "", true);
    AddSpec(specs, "closesplit", "Close Editor Split", "Close the second editor pane", "", "", true);
    AddSpec(specs, "detachoutputwindow", "Detach Output to Native Window", "Move Output to an independent multi-monitor window", "", "", true);
    AddSpec(specs, "statusbar", "Toggle Status Bar", "Show or hide the status bar", "", "", true);
    AddSpec(specs, "zoomin", "Zoom In", "Increase editor font size", "Ctrl+=", "", true);
    AddSpec(specs, "zoomout", "Zoom Out", "Decrease editor font size", "Ctrl+-", "", true);
    AddSpec(specs, "zoomreset", "Reset Zoom", "Reset editor font size to default", "Ctrl+0", "", true);
    AddSpec(specs, "uizoomin", "UI Zoom In", "Enlarge the whole interface", "Ctrl+Shift+=", "", true);
    AddSpec(specs, "uizoomout", "UI Zoom Out", "Shrink the whole interface", "Ctrl+Shift+-", "", true);
    AddSpec(specs, "uizoomreset", "Reset UI Zoom", "Reset the interface scale to 100%", "Ctrl+Shift+0", "", true);
    AddSpec(specs, "toggletheme", "Toggle Theme", "Switch between dark and light theme", "Ctrl+Shift+T", "", true);
    AddSpec(specs, "fullscreen", "Toggle Fullscreen", "Enter or exit fullscreen mode", "F11", "", true);
    AddSpec(specs, "minimap", "Toggle Minimap", "Show or hide the minimap panel", "Ctrl+Shift+M", "", true);
    AddSpec(specs, "settings", "Settings", "View and adjust IDE settings", "Ctrl+,", "", true);
    AddSpec(specs, "keybindings", "Keyboard Shortcuts", "Show active keyboard shortcuts and conflicts", "Ctrl+K Ctrl+S", "", true);
    AddSpec(specs, "copytoolrow", "Copy Tool Row", "Copy the selected row from the active tool panel", "Ctrl+Alt+Y", "", true);
    AddSpec(specs, "copyoutputtext", "Copy Output Text", "Copy the full build output buffer", "Ctrl+Alt+Shift+Y", "", true);
    AddSpec(specs, "clearoutput", "Clear Output", "Clear the build output panel", "Ctrl+Alt+L", "", true);
    AddSpec(specs, "filteroutput", "Filter Output", "Filter build output rows by text", "Ctrl+Alt+F", "", true);
    AddSpec(specs, "toggleoutputwrap", "Toggle Output Wrap", "Wrap long output rows into readable chunks", "Ctrl+Alt+W", "", true);
    AddSpec(specs, "toggleoutputautoscroll", "Toggle Output Auto-Scroll", "Keep new output pinned or preserve the current view", "Ctrl+Alt+S", "", true);
    AddSpec(specs, "treenewfile", "Explorer: New File", "Create a file in the selected project folder", "Ctrl+Alt+N", "", true);
    AddSpec(specs, "treenewfolder", "Explorer: New Folder", "Create a folder in the selected project folder", "Ctrl+Alt+Shift+N", "", true);
    AddSpec(specs, "treerename", "Explorer: Rename", "Rename the selected project tree item", "Ctrl+Alt+R", "", true);
    AddSpec(specs, "treedelete", "Explorer: Delete", "Delete the selected project tree item", "Ctrl+Alt+D", "", true);
    AddSpec(specs, "treerefresh", "Explorer: Refresh", "Refresh the project tree", "Ctrl+Alt+Shift+R", "", true);
    AddSpec(specs, "quickopen", "Quick Open File", "Open a project file by name or path", "Ctrl+P", "", true);
    AddSpec(specs, "undo", "Undo", "Undo last action", "Ctrl+Z", "", true);
    AddSpec(specs, "redo", "Redo", "Redo last undone action", "Ctrl+Y", "", true);
    AddSpec(specs, "cut", "Cut", "Cut selection", "Ctrl+X", "", false);
    AddSpec(specs, "copy", "Copy", "Copy selection", "Ctrl+C", "", false);
    AddSpec(specs, "paste", "Paste", "Paste clipboard", "Ctrl+V", "", false);
    AddSpec(specs, "selectall", "Select All", "Select the full document", "Ctrl+A", "", true);
    AddSpec(specs, "gotodef", "Go To Definition", "Jump to symbol definition", "F12", "definition", true);
    AddSpec(specs, "findrefs", "Find References", "Find semantic references", "Shift+F12", "references", true);
    AddSpec(specs, "incomingcalls", "Incoming Calls", "Show callers of the symbol at the cursor", "Ctrl+Alt+H", "references", true);
    AddSpec(specs, "outgoingcalls", "Outgoing Calls", "Show calls made by the enclosing function", "Ctrl+Alt+Shift+H", "definition", true);
    AddSpec(specs, "rename", "Rename Symbol", "Rename a semantic symbol", "F2", "rename", true);
    AddSpec(specs, "nexterror", "Next Error", "Jump to next diagnostic", "F8", "diagnostics", true);
    AddSpec(specs, "preverror", "Previous Error", "Jump to previous diagnostic", "Shift+F8", "diagnostics", true);
    AddSpec(specs, "checknow", "Run Check Now", "Run diagnostics for the active file now", "Ctrl+Alt+C", "diagnostics", true);
    AddSpec(specs, "searchproject", "Search in Project", "Find text across project files", "Ctrl+Shift+F", "", true);
    AddSpec(specs, "workspacesymbols", "Workspace Symbols", "Find symbols across project files", "Ctrl+T", "symbols", true);
    AddSpec(specs, "symboloutline", "Symbol Outline", "Toggle document symbol list", "Ctrl+Shift+O", "symbols", true);
    AddSpec(specs, "completion_trigger", "Trigger Completion", "Show completion suggestions", "Ctrl+Space", "completion", false);
    AddSpec(specs, "signature_trigger", "Trigger Signature Help", "Show signature help at the cursor", "Ctrl+Shift+Space", "signature", true);
    AddSpec(specs, "signaturenextoverload", "Signature: Next Overload", "Show the next signature overload", "Ctrl+Alt+Down", "signature", true);
    AddSpec(specs, "signatureprevoverload", "Signature: Previous Overload", "Show the previous signature overload", "Ctrl+Alt+Up", "signature", true);
    AddSpec(specs, "addnextoccurrence", "Add Next Occurrence", "Add a cursor at the next matching word", "Ctrl+D", "", true);
    AddSpec(specs, "skipoccurrence", "Skip Occurrence", "Move to the next matching word", "Ctrl+K Ctrl+D", "", true);
    AddSpec(specs, "selectalloccurrences", "Select All Occurrences", "Select every matching word", "Ctrl+Shift+L", "", true);
    AddSpec(specs, "clearcursors", "Clear Extra Cursors", "Remove secondary cursors", "Escape", "", true);
    AddSpec(specs, "expandselection", "Expand Selection", "Grow selection from word to line to document", "Shift+Alt+Right", "", true);
    AddSpec(specs, "shrinkselection", "Shrink Selection", "Shrink selection from document to line to word", "Shift+Alt+Left", "", true);
    AddSpec(specs, "togglelinecomment", "Toggle Line Comment", "Comment or uncomment the active line", "Ctrl+/", "", true);
    AddSpec(specs, "toggleblockcomment", "Toggle Block Comment", "Wrap or unwrap the selection in a block comment", "Shift+Alt+A", "", true);
    AddSpec(specs, "duplicateline", "Duplicate Selection", "Duplicate the active text line or visual-scene selection", "Ctrl+Shift+D", "", true);
    AddSpec(specs, "deletesceneselection", "Delete Scene Selection", "Delete the selected 2D objects or 3D hierarchy roots", "Delete", "", true);
    AddSpec(specs, "movelineup", "Move Line Up", "Move the active line above the previous line", "Alt+Up", "", true);
    AddSpec(specs, "movelinedown", "Move Line Down", "Move the active line below the next line", "Alt+Down", "", true);
    AddSpec(specs, "organizebinds", "Organize Binds", "Sort and deduplicate the leading Zia bind block", "Shift+Alt+O", "diagnostics", true);
    AddSpec(specs, "extractlocal", "Extract Local Variable", "Extract the selected expression into a simple local variable", "", "rename", true);
    AddSpec(specs, "extractfunction", "Extract Function", "Extract selected complete Zia statements into a new function", "", "rename", true);
    AddSpec(specs, "inlinelocal", "Inline Local Variable", "Inline a simple single-assignment local variable", "Ctrl+Alt+I", "rename", true);
    AddSpec(specs, "suppressdiagnostic", "Suppress Warning", "Insert a Zia suppression comment for the warning at the cursor", "", "diagnostics", true);
    AddSpec(specs, "applydiagnosticfix", "Apply Diagnostic Fix-It", "Apply the first language-proposed diagnostic fix at the cursor", "Ctrl+.", "diagnostics", true);
    AddSpec(specs, "createmissingbind", "Create Missing Bind", "Add a safe runtime bind for an undefined Zanna namespace alias", "", "diagnostics", true);
    AddSpec(specs, "trimtrailingwhitespace", "Trim Trailing Whitespace", "Remove trailing spaces and tabs from the active document", "Shift+Alt+T", "", true);
    AddSpec(specs, "togglerenderwhitespace", "Toggle Render Whitespace", "Cycle whitespace markers: off, boundary, all", "", "", true);
    AddSpec(specs, "formatdocument", "Format Document", "Format the active document when a formatter is available", "Shift+Alt+F", "", true);
    AddSpec(specs, "formatselection", "Format Selection", "Format the selected text when a formatter is available", "", "", true);
    return specs;
}

/// @brief Append one command specification to the catalog list.
/// @param specs Mutable list that receives the command specification.
/// @param id Stable command id used by shortcut dispatch and command handlers.
/// @param label Human-readable command name shown in the palette.
/// @param description Short explanatory text for future UI/help surfaces.
/// @param shortcut Default keyboard shortcut, or an empty string when unset.
/// @param capability Language-service capability required to enable the command.
/// @param showInPalette Whether the command should appear in the command palette.
/// @details This helper keeps each catalog row concise while still assigning
///          every field explicitly. That is easier to teach and review than
///          positional object construction sprinkled through registry code.
func AddSpec(specs: List[CommandSpec],
             id: String,
             label: String,
             description: String,
             shortcut: String,
             capability: String,
             showInPalette: Boolean) {
    var spec = new CommandSpec();
    spec.id = id;
    spec.label = label;
    spec.description = description;
    spec.category = CategoryForCommand(id);
    spec.shortcut = PlatformShortcut(shortcut);
    spec.capability = capability;
    spec.showInPalette = showInPalette;
    specs.add(spec);
}

/// @brief Materialize a canonical shortcut for the current desktop platform.
/// @details Studio's catalog uses Ctrl as its platform-primary modifier. Cocoa
///          menus have always translated that token to Command, but global
///          shortcuts and custom-drawn palette/context labels did not. Keeping
///          the conversion here makes behavior and presentation agree. User
///          overrides bypass this helper so a deliberately physical Ctrl chord
///          remains possible on macOS.
/// @param shortcut Canonical catalog shortcut, possibly a two-part chord.
/// @return Command-based spelling on macOS; unchanged elsewhere.
expose func PlatformShortcut(shortcut: String) -> String {
    if Machine.Os == "macos" {
        return Str.Replace(shortcut, "Ctrl+", "Cmd+");
    }
    return shortcut;
}

/// @brief Return the command-palette category for a command id.
/// @details Categories are part of the command catalog because they are static
///          command metadata. Keeping this classifier next to DefaultCommandSpecs
///          lets developers review ids, labels, shortcuts, capabilities, and
///          palette grouping in one place before they touch runtime dispatch.
/// @param id Stable command id from the catalog.
/// @return Palette category name such as File, Edit, Build, or Debug.
expose func CategoryForCommand(id: String) -> String {
    if id == "new" or id == "newscene2d" or id == "newscene3d"
       or id == "open" or id == "openfolder"
       or id == "addfolder" or id == "removefolder" or id == "closeworkspace"
       or id == "save" or id == "saveas" or id == "saveall"
       or id == "close" or id == "reload" or id == "recentfiles"
       or id == "reopenclosed" {
        return "File";
    }
    if id == "find" or id == "searchproject" {
        return "Search";
    }
    if id == "quickopen" or id == "goto" or id == "gotodef"
       or id == "findrefs" or id == "incomingcalls" or id == "outgoingcalls"
       or id == "nexterror" or id == "preverror" or id == "workspacesymbols"
       or id == "symboloutline" or id == "signature_trigger"
       or id == "signaturenextoverload" or id == "signatureprevoverload" {
        return "Navigate";
    }
    if id == "rename" or id == "organizebinds" or id == "extractlocal"
       or id == "extractfunction" or id == "inlinelocal"
       or id == "suppressdiagnostic" or id == "applydiagnosticfix"
       or id == "createmissingbind" {
        return "Refactor";
    }
    if id == "build" or id == "buildrun" or id == "run" or id == "runscene" or id == "stopjob" {
        return "Build";
    }
    if Str.StartsWith(id, "debug") or id == "togglebreakpoint" {
        return "Debug";
    }
    if id == "togglesidebar" or id == "movesidebarleft"
       or id == "movesidebarright" or id == "dockpanelsleft"
       or id == "dockpanelsbottom" or id == "dockpanelsright"
       or id == "floatpanelgroup"
       or id == "movetoolleft" or id == "movetoolbottom"
       or id == "movetoolright" or id == "movetoolfloating"
       or id == "scenefocus"
       or id == "statusbar" or id == "zoomin"
       or id == "zoomout" or id == "zoomreset" or id == "uizoomin"
       or id == "uizoomout" or id == "uizoomreset"
       or id == "toggletheme" or id == "fullscreen" or id == "minimap"
       or id == "resetworkspacelayout"
       or id == "spliteditorright" or id == "focusotherpane" or id == "closesplit"
       or id == "detachoutputwindow" {
        return "View";
    }
    if id == "settings" or id == "keybindings" {
        return "Settings";
    }
    if id == "copytoolrow" or id == "copyoutputtext" or id == "clearoutput"
       or id == "filteroutput" or id == "toggleoutputwrap"
       or id == "toggleoutputautoscroll" {
        return "Output";
    }
    if Str.StartsWith(id, "tree") {
        return "Explorer";
    }
    if id == "undo" or id == "redo" or id == "cut" or id == "copy"
       or id == "paste" or id == "selectall" or id == "completion_trigger"
       or id == "addnextoccurrence" or id == "skipoccurrence"
       or id == "selectalloccurrences" or id == "clearcursors"
       or id == "expandselection" or id == "shrinkselection"
       or id == "togglelinecomment" or id == "toggleblockcomment"
       or id == "duplicateline" or id == "deletesceneselection"
       or id == "movelineup" or id == "movelinedown"
       or id == "trimtrailingwhitespace" or id == "formatdocument"
       or id == "formatselection" {
        return "Edit";
    }
    return "Command";
}
