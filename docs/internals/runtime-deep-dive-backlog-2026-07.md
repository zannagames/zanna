---
status: active
audience: contributors
last-verified: 2026-07-30
---

# Runtime Deep-Dive Backlog — July 2026

This is the durable backlog for the July 2026 review of Zanna's core runtime,
memory management, threads, collections, I/O, archives, networking,
localization, audio, input, GUI, crypto, and common-utility paths. It
supplements rather than replaces
`runtime-hardening-audit-2026-07.md`: that older ledger records 64 previously
closed findings, while the entries below come from the current source review.

The identifiers are stable. A `Fixed-WT` item has an implementation and focused
regression coverage in the shared working tree, but is not claimed to be
committed or released. `Open` means the source evidence is strong enough for
implementation. `Measure` is a performance recommendation that requires a
focused benchmark before changing policy. `ADR` means the likely fix changes a
public/runtime contract and therefore needs an ADR under `AGENTS.md`.

Priorities are: P0 memory safety or state corruption; P1 correctness,
concurrency, or unbounded-resource risk; P2 material performance/resilience;
P3 maintainability, diagnostics, or additional assurance.

Current ledger snapshot: **264 recommendations** — 155 `Fixed-WT`, 56 `Open`,
38 `Measure`, and 15 `ADR`; 104 are P0.

## Core memory, GC, pools, traps, and strings

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-001 | Fixed-WT | P1 | `rt_heap_realloc` in `core/rt_heap.c` checked `elem_size == 0` before normalizing `cap = max(new_len, new_cap)`. | Validate zero-sized elements against the effective capacity so `new_len > new_cap` cannot create a non-empty zero-stride allocation. |
| ZRT-002 | Fixed-WT | P0 | The locked free-list branch of `pop_from_freelist` changed block state after dropping `freelist_lock`. | Claim the block and decrement `free_count` while the list lock still protects the head, preventing a shutdown/free race from observing contradictory state. |
| ZRT-003 | Fixed-WT | P0 | Locked `push_to_freelist` published the node before updating `free_count`, while lifecycle code reads both. | Publish the node and update the checked counter in one lock-protected transaction. |
| ZRT-004 | Fixed-WT | P1 | `rt_pool_stats` accepted a signed enum but rejected only values at or above `RT_POOL_COUNT`. | Reject negative size-class values as well as oversized values. |
| ZRT-005 | Fixed-WT | P0 | Heap-backed string helpers trusted `s->heap` and could dereference a forged or stale wrapper/header pair. | Resolve `s->data` through the live heap registry and require the registered header to equal the wrapper header before reading it. |
| ZRT-006 | Fixed-WT | P0 | Many functions in `rt_string_ops.c`, `rt_string_advanced.c`, and `rt_string_specialized.c` read wrapper fields before validating the string handle. | Validate every non-null string argument before dereference and return immediately if a configured trap hook returns. |
| ZRT-007 | Open | P2 | `g_heap_registry_` in `rt_heap.c` uses one process-wide spin lock for lookup, retain, release, allocation registration, and deletion. | Shard the live-payload registry by mixed pointer hash, including independent locks/counters, then benchmark high-thread-count retain/release. |
| ZRT-008 | Open | P2 | `rt_heap_registry_ensure_capacity_locked_` grows when live entries plus tombstones cross 5/8, even when a same-capacity rehash would remove enough tombstones. | Add tombstone-dominance compaction before geometric growth to reduce memory and probe length in churn-heavy workloads. |
| ZRT-009 | Open | P2 | `rt_heap_get_containing_info` scans every registry slot for interior-pointer queries. | Add a range index or page-to-allocation side table so interior validation is not O(total live allocations). |
| ZRT-010 | ADR | P0 | `rt_heap_try_get_header` returns a borrowed header after releasing the registry lock; another owner can release the allocation before a caller dereferences it. | Replace cross-module borrowed-header use with lock-bounded value snapshots or a pin/unpin API; document the new lifetime contract in an ADR. |
| ZRT-011 | Open | P2 | `rt_heap_realloc` allocates the replacement before validating that the old payload is live and uniquely owned. | Validate existence, kind, and exclusive ownership before reserving replacement storage, while preserving race safety through an explicit reservation state. |
| ZRT-012 | Open | P1 | `rt_heap_realloc` holds the global registry lock while copying the old payload into the new allocation and moving registry state. | Introduce a relocating/reserved state so potentially large copies happen outside the registry lock without allowing retain/release of the old address. |
| ZRT-013 | Measure | P2 | `rt_heap_alloc_zeroed_` zeroes all managed capacity, including capacity not yet logically observable. | Benchmark a split zeroed/uninitialized internal allocator and zero only newly observable regions; retain zeroed allocation for public safety-sensitive paths. |
| ZRT-014 | Open | P2 | Every managed graph mutation shares the single `g_gc_world_lock` read side in `rt_gc.c`; collection takes it exclusively. | Measure lock contention and prototype epoch/handshake-based quiescence so unrelated mutators do not contend on one RW lock. |
| ZRT-015 | Open | P1 | `rt_gc_collect` stops mutators before allocating the snapshot sized from `g_gc.count`. | Reserve/reuse snapshot capacity before acquiring the exclusive graph barrier and revalidate the count after quiescence. |
| ZRT-016 | Open | P2 | GC traversal grows `gc_edge_list` and snapshot work arrays with `realloc` during the stop-the-world interval. | Reuse bounded scratch arenas or pre-size from tracked edge metadata to keep allocator latency out of the pause. |
| ZRT-017 | ADR | P2 | `rt_gc_collect` performs a complete synchronous trial-deletion pass once requested. | Define an incremental or time-budgeted collection contract with explicit safe-point progress and an ADR for observable scheduling semantics. |
| ZRT-018 | Open | P1 | `gc_entry.survived` is `uint16_t`; long-lived entries can eventually wrap unless every increment saturates. | Centralize a saturating increment and add a regression that drives the counter through its maximum. |
| ZRT-019 | Open | P2 | Weak-reference buckets allocate one `weak_chain` node per registration and use linked chains. | Use slabbed/open-addressed weak registration storage and reuse nodes to reduce allocator pressure during weak-reference churn. |
| ZRT-020 | Open | P2 | Each collection builds a fresh open-addressed garbage membership set with `calloc`. | Cache and clear reusable membership storage, with an upper retention bound, to avoid per-pass allocation and zeroing. |
| ZRT-021 | Open | P1 | Reclaim-phase finalizers run while the exclusive managed-graph barrier remains held. | Split logical graph detachment from user finalization where possible, so slow/reentrant finalizers do not extend global pause time; preserve resurrection semantics with tests. |
| ZRT-022 | Measure | P2 | Pool slabs remain attached until shutdown even when every block in a slab is free. | Add an opt-in trim policy that reclaims fully free excess slabs after a cold interval; benchmark fragmentation and synchronization cost. |
| ZRT-023 | Measure | P2 | `rt_pool_alloc` clears the entire selected size class, not merely the requested bytes. | Measure clearing requested size plus metadata-safe tail poisoning in debug builds, keeping deterministic zero semantics where callers require them. |
| ZRT-024 | Open | P3 | Pool fallback allocations are excluded from per-class stats, so oversize and exhaustion behavior is invisible. | Add separate checked fallback allocation/free byte counters and expose them through diagnostics without changing existing class totals. |
| ZRT-025 | Fixed-WT | P1 | Pool free accepted both a pointer and caller-supplied size but ignored disagreements within or across private size classes. | Record the exact live request in the header's freelist/request union, distinguish exact-size and class mismatches, trap before state/statistics mutation, and keep rejected allocations retryable with their original sizes. |
| ZRT-202 | Fixed-WT | P0 | `rt_pool_free` chose `free(ptr)` versus private-header inspection solely from the caller-supplied size: an oversized size for a pooled payload passed an interior pointer to `free`, while a small size for a fallback allocation read before the allocation as if metadata existed. | Give system fallbacks the same maximally aligned private header as slabs, classify null-owner provenance before trusting size, free the complete header allocation, and verify both cross-boundary mismatch directions plus fallback alignment and heap-layer behavior. |

## Threads, synchronization, futures, schedulers, and work queues

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-026 | Fixed-WT | P0 | Scheduler name paths could continue after invalid-handle traps and then read string wrapper data. | Make `scheduler_name_valid` status-returning and stop every schedule/cancel/query path after a returning trap. |
| ZRT-027 | Fixed-WT | P1 | Scheduler insertion and replacement could leak retained names or alter pending counts after allocation/retain failure. | Stage ownership before publication and update the pending counter only with the linked-list mutation. |
| ZRT-028 | Fixed-WT | P1 | Thread-pool timeout arithmetic could overflow and POSIX wait errors were not consistently distinguished from timeout. | Use checked deadline math and convert native errors into deterministic traps while preserving timeout as a normal result. |
| ZRT-029 | Fixed-WT | P0 | Concurrent-map replacement and insertion could continue after a returning retain/allocation trap. | Make mutations transactional: prepare key/value ownership first, publish once, and release displaced state outside the lock. |
| ZRT-030 | Open | P1 | Cancellation delivery in `rt_threads_common.c` can invoke cleanup that reaches another cancellation point while cancellation is still being processed. | Add a thread-local cancellation-delivery guard and defer nested delivery until the outer cleanup completes. |
| ZRT-031 | Open | P1 | Several public thread object entry points retain the receiver through helpers that can trap, but the returning-trap contract is enforced inconsistently. | Audit all retain/validate helpers and require explicit status results plus immediate exits on trap return. |
| ZRT-032 | Measure | P2 | Each Debouncer instance owns a monitoring thread rather than sharing a timing service. | Benchmark a process/context timer wheel serving many debouncers, with cancellation generations to prevent stale callbacks. |
| ZRT-033 | Open | P3 | Monitor, Future, Channel, ConcurrentQueue, Scheduler, and ThreadPool each implement similar clock/deadline conversion code. | Consolidate checked monotonic deadlines and remaining-time conversion into one internal cross-platform helper and one conformance test matrix. |
| ZRT-034 | Open | P1 | Windows scheduler time conversion multiplies performance-counter ticks before division, allowing intermediate overflow on long uptimes/high frequencies. | Use quotient/remainder conversion or 128-bit-capable checked arithmetic through the platform capability layer. |
| ZRT-035 | Measure | P2 | `rt_scheduler_is_due`, `_cancel`, `_generation_of`, and scheduling walk a name-keyed linked list. | Replace the list with a hash index plus due-time min-heap once benchmarks show meaningful scheduler cardinality. |
| ZRT-036 | Open | P2 | Due checks first find a named entry and later cleanup scans expired entries again. | Combine lookup and expiry maintenance or perform bounded lazy expiry from the earliest-deadline structure. |
| ZRT-037 | Measure | P2 | Scheduler name comparisons repeatedly validate strings and query their lengths through registries inside list scans. | Store validated byte length and a cached hash in each entry, retaining the name solely for ownership/publication. |
| ZRT-038 | Open | P1 | POSIX scheduler macros wrap mutex operations without checking every `pthread_mutex_*` return. | Replace macros with status-checking helpers that trap or abort consistently according to recoverability. |
| ZRT-039 | Measure | P2 | `rt_concmap.c` serializes all keys through one object mutex and performs resizing under that mutex. | Prototype lock striping with generation-stamped table replacement; benchmark read-heavy and disjoint-key writers. |
| ZRT-040 | Open | P1 | Concurrent-map snapshot/key extraction allocates managed/native storage while holding the map lock. | Count and reserve outside the lock, retry on generation change, and keep managed allocation/traps out of the critical section. |
| ZRT-041 | Open | P2 | A failed concurrent-map resize leaves the old high-load table in service, so chains can continue degrading indefinitely. | Record resize debt and retry with bounded backoff on later mutations; expose the failure in diagnostics. |
| ZRT-042 | Open | P1 | Channel send/receive paths retain or release managed values while holding the channel monitor, allowing finalizers/traps to prolong or reenter the critical section. | Stage retains before locking and detach transferred/released values under lock for destruction afterward. |
| ZRT-043 | Measure | P2 | Channel close and several state transitions wake every waiter with broadcast even when only one sender/receiver can progress. | Separate sender/receiver conditions or add waiter counts so normal progress signals one side and close alone broadcasts. |
| ZRT-044 | Measure | P2 | Every ConcurrentQueue enqueue performs a standalone `malloc` for `cq_node` and every dequeue frees it. | Add a bounded node cache or pool class and benchmark producer/consumer throughput plus retained-memory cost. |
| ZRT-045 | ADR | P1 | Pointer-returning ConcurrentQueue dequeue APIs use null for closed, timeout, empty, and a queued null value; only Option variants disambiguate some paths. | Define a status-plus-output API for all blocking modes and specify compatibility in an ADR. |
| ZRT-046 | Open | P1 | ConcurrentQueue and Future can fall back from monotonic initialization to realtime, while callers assume timeout immunity to wall-clock changes. | Carry the actual selected clock through deadline construction/wait and add clock-jump tests for fallback paths. |
| ZRT-047 | Measure | P2 | ThreadPool uses one strict FIFO protected by one monitor; all workers contend on the same queue. | Benchmark per-worker deques with work stealing while preserving FIFO only where the public contract promises it. |
| ZRT-048 | ADR | P1 | ThreadPool construction rejects sizes above the hard-coded 1024-worker ceiling without exposing a platform-derived limit. | Specify a documented capability/error contract and derive safe limits from platform resources in an ADR. |
| ZRT-049 | Open | P3 | ThreadPool submission returns one false result for shutdown, queue/allocation failure, invalid callback, and native synchronization errors. | Add internal structured failure reasons and surface them through diagnostics while preserving the Boolean compatibility wrapper. |
| ZRT-050 | Open | P1 | Some ThreadPool queue/state invariants remain protected only by assertions, which disappear in release builds. | Convert externally triggerable count/state violations into checked aborts or traps and fuzz shutdown/submit interleavings. |

## Collections and containers

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-051 | Fixed-WT | P0 | Bag scalar and set-operation paths could continue after an invalid receiver/key trap and dereference null or forged state. | Make receiver/key extraction status-returning and stop Add/Remove/Has/Items/Union/Intersect/Diff/Clone after failure. |
| ZRT-052 | Fixed-WT | P1 | BiMap doubled forward/inverse capacities without explicit overflow guards and did not uniformly stop after invalid key/value traps. | Check both capacity multiplications and maximum count, and validate both string sides before any table mutation. |
| ZRT-053 | Fixed-WT | P0 | `rt_binbuf_write_str` read an invalid string after a returning trap; `rt_binbuf_read_bytes` could continue after output allocation failure. | Validate the string before length/data access and return before copying into a missing Bytes result. |
| ZRT-054 | Fixed-WT | P0 | Scalar queries such as `rt_bitset_len` used `as_bitset(...)->field` directly, so a returning invalid-object trap became a null dereference. | Store the validated receiver and return a neutral scalar when a trap hook returns. |
| ZRT-055 | Fixed-WT | P0 | BloomFilter Add/MightContain used string data and length without rejecting stale/forged handles under returning traps. | Route both operations through a live-string data extractor and stop on failure. |
| ZRT-056 | Fixed-WT | P0 | Bytes string/hex/base64 constructors normalized some invalid handles to empty input and had unchecked output-size conversions. | Distinguish null from invalid handles, guard `size_t` to `int64_t` conversion, and clean partial results on every decode failure. |
| ZRT-057 | Fixed-WT | P1 | CountMap resize and load calculations could overflow, while key paths could continue after a returning trap. | Use checked geometric growth/load arithmetic, cap count, and make key extraction a required status. |
| ZRT-058 | Fixed-WT | P0 | DefaultMap constructor/set/snapshot paths could publish or leak partial ownership if a managed retain or sequence append trapped and returned. | Wrap retains/appends in local recovery, roll back staged key/value/snapshot state, then re-raise and return. |
| ZRT-059 | Fixed-WT | P0 | FrozenMap insertion/replacement retained key/value parts separately without complete rollback when the second operation trapped. | Treat slot ownership as a two-reference transaction and propagate insertion failure through constructors and merge. |
| ZRT-060 | Fixed-WT | P0 | FrozenSet construction and lookup accepted non-null invalid string elements far enough to read wrapper data. | Validate each extracted element and clean the partially built immutable table on failure. |
| ZRT-061 | Fixed-WT | P0 | Iterator constructors/operations could leak temporary managed values when adapter allocation or publication trapped. | Keep temporary ownership until iterator publication succeeds and release it in recovery paths. |
| ZRT-062 | Fixed-WT | P0 | List, Queue, Stack, Seq, SparseArray, and related scalar getters directly dereferenced the result of `as_*` validation. | Store the validated receiver and provide a neutral return after returning traps rather than dereferencing null. |
| ZRT-063 | Fixed-WT | P0 | LRU cache insertion/replacement and snapshot paths had multi-owner retain/append sequences without full returning-trap rollback. | Stage key/value/node ownership transactionally, publish complete replacement/eviction state before callback-capable release, prove void-returning snapshot appends changed length, and destroy detached entries/snapshots only after leaving table mutation. |
| ZRT-064 | Fixed-WT | P1 | OrderedMap lacked consistent capacity/count overflow checks and continued after invalid key extraction in several paths. | Guard maximum count/geometric resize and require successful receiver/table/key validation for every operation. |
| ZRT-065 | Fixed-WT | P0 | MultiMap could publish a new per-key sequence before all key/value retains/appends succeeded. | Build the new sequence and key entry off-table, then publish atomically or release the entire staged entry. |
| ZRT-066 | Fixed-WT | P0 | TreeMap key/value snapshots leaked their partial sequence if sequence growth/retain trapped and returned. | Make append helpers consume the partial snapshot on failure, re-raise, and force callers to stop. |
| ZRT-067 | Fixed-WT | P0 | Trie recursive cleanup could exhaust the C stack, and clone/key collection lost partial state on returning traps. | Free subtrees iteratively without cleanup allocation; use parent-linked, allocation-free walks for tracing/collection/clone traversal and recovery-owned construction state for every fallible value/node publication. |
| ZRT-068 | Fixed-WT | P0 | UnionFind Find/Union/Reset and scalar queries could continue after invalid receiver/index traps. | Stop before parent/rank array access and return neutral values when a configured trap returns. |
| ZRT-069 | Fixed-WT | P0 | WeakMap trusted retained string keys during probing, used overflow-prone load math, and could corrupt ownership on failed rehash/snapshot append. | Revalidate keys, use checked thresholds/power-of-two invariants, rebuild transactionally, and release partial snapshots. |
| ZRT-070 | Fixed-WT | P1 | Seq sort scratch allocation multiplied logical length by pointer size without an explicit overflow guard. | Reject impossible scratch sizes before allocation and copy only the merged range back from temporary storage. |
| ZRT-071 | Fixed-WT | P1 | BinaryBuffer, BloomFilter, Bytes, Map variants, and Trie used inconsistent null-versus-invalid semantics for strings. | Preserve null as each API documents, but trap and stop for every non-null unregistered handle. |
| ZRT-072 | Open | P2 | Bag, BiMap, CountMap, DefaultMap, FrozenMap/Set, OrderedMap, TreeMap, Trie, WeakMap, and others each reimplement string validation plus data/length lookup. | Add one internal lock-bounded borrowed-string-view helper to remove duplication and repeated registry probes. |
| ZRT-073 | Open | P0 | DefaultMap/BiMap/CountMap/WeakMap now join the other map families in the manual function-by-function transaction audit and focused invalid/retain/reentrancy coverage, but direct native allocators still prevent deterministic fail-at-N sweeps and ZRT-263/ZRT-264 identify the remaining trap-drain and resurrection shapes. | Add the native allocation fault shim in ZRT-244, complete the cross-family finalizer follow-ups in ZRT-263/ZRT-264, then sweep constructor, resize, replacement, remove, snapshot, clone, compaction, Clear, and finalization failures uniformly. |
| ZRT-074 | Open | P1 | Immutable map/set snapshot builders allocate managed sequences one element at a time and recovery coverage differs by operation. | Introduce one trap-safe snapshot builder with pre-reserve, ownership transfer, and a single cleanup path. |
| ZRT-075 | Fixed-WT | P0 | Frozen collection element extraction treated every non-string sequence element as a boxed string and, after a returning unbox trap, silently skipped it as though it were a legitimate null. | Use status-returning raw/boxed/null extraction, reject wrong boxes, ordinary objects, and forged pointers, and transactionally destroy the partial FrozenMap/FrozenSet before propagating the trap. |
| ZRT-206 | Fixed-WT | P0 | `rt_unbox_str` and `rt_box_try_to_str` ignored the result of the contained-string retain; heap-backed `rt_string_ref` itself returned the input after a returning heap-retain overflow, so callers could receive an unowned pointer and `ToStrOption` could normalize the fault to None. | Require successful SSO and heap-backed retains before publishing a String, return null/failure after returning traps, and keep structural mismatch distinct from retain failure in the Option wrapper. |
| ZRT-207 | Fixed-WT | P0 | FrozenMap/FrozenSet constructors queried non-null source handles through `rt_seq_len`; its safe post-trap zero sentinel converted a wrong-class source into a successful empty immutable collection. | Add one private sized Seq-identity predicate and require every non-null constructor source to pass it before length/allocation work. |
| ZRT-208 | Fixed-WT | P0 | Owning Seq Set/Push/Insert published after a failed retain, Pop/Remove mutated before proving caller ownership, and PushAll could leave an incremented length plus earlier speculative retains when a later retain trapped and returned. | Use one status-returning managed-value retain primitive, keep visible length/content unchanged until ownership succeeds, and roll back every staged bulk retain before propagating failure. |
| ZRT-209 | Fixed-WT | P0 | Seq Slice/Clone dereferenced the null result of receiver validation under a returning trap, and element-by-element owning slice construction could return or leak a partial result after a retain failure. | Guard validation/allocation results and build owning slices through an all-or-nothing retained range transaction that consumes unpublished results on failure. |
| ZRT-210 | Fixed-WT | P0 | MultiMap incremented `total_count` after an existing-key `Seq.Push` even when the owning append's retain trapped and returned, producing metadata/content disagreement. | Recover around the published per-key Seq append, verify its void API advanced length by exactly one, and advance aggregate count only after that postcondition commits. |
| ZRT-211 | Fixed-WT | P0 | IntMap replacement/insertion ignored failed retains; Get/GetOr/Has dereferenced a failed checked cast, while Keys/Values could dereference invalid receivers or return partial owning snapshots. | Require checked ownership before mutation, guard every receiver/table result, and consume boxed-key/value snapshots through trap-protected append helpers. |
| ZRT-212 | Fixed-WT | P0 | Map.SetIfMissing and MultiMap.GetFirst bypassed checked ownership: one could publish an unowned value, while the other could return a pointer without acquiring the documented caller reference. | Route both through the shared status-returning collection retain helper and stop before publication/return when ownership cannot be acquired. |
| ZRT-213 | Fixed-WT | P0 | `Option.Some` ignored a returning pointer-retain trap before installing its allocation recovery, `Option.SomeStr` could normalize a failed string retain to `Some(NULL)`, and `Option.None` dereferenced a null allocation result. | Make pointer/string ownership acquisition status-returning before publication and require successful Option allocation before initializing any variant fields. |
| ZRT-214 | Fixed-WT | P0 | Queue, Stack, Ring, PriorityQueue, Deque, List, and Set publication/non-destructive result paths ignored failed managed retains, while several destructive reads performed a redundant retain/release instead of moving the owning container's existing reference; failures could publish an unowned value, drop a result, or reject a legal saturated-reference Pop. | Require checked ownership for insertion, replacement, peek, and get; implement owning Pop/Remove as a refcount-neutral transfer; and leave length, slots, and old values unchanged whenever genuinely new ownership cannot be acquired. |
| ZRT-215 | Fixed-WT | P0 | Queue, Stack, Deque, and PriorityQueue Option-pop wrappers removed the stored element before proving that `Option.Some` could retain it, so retain exhaustion could lose the element and be normalized into a null/absent-looking result. | Construct the Option from the borrowed stored value before mutating the container, then release the container's reference only after successful Option publication. |
| ZRT-216 | Fixed-WT | P0 | Queue, Stack, Ring, and Deque clones plus PriorityQueue-to-Seq conversion could leak or return a partial result after a retaining append/allocation trap returned. | Give each conversion a local recovery owner, pre-reserve known output cardinality, consume every partial native/managed result, and propagate the original trap without changing the source. |
| ZRT-217 | Fixed-WT | P0 | SparseArray insertion/replacement ignored failed retains, finalization/traversal could continue after a failed cast, and index/value snapshots could dereference invalid receivers or leak partially built sequences. | Make slot publication ownership-checked, guard internal receiver casts, pre-reserve snapshots, and destroy partial results through one trap-recovery path. |
| ZRT-218 | Fixed-WT | P0 | Deque non-throwing pop variants mutated retained graph edges without entering the GC mutator scope used by the ordinary push/pop operations. | Enclose validation, result-retain acquisition, slot removal, and stored-reference release in one balanced mutator region on every return path. |
| ZRT-219 | Fixed-WT | P0 | Object-array Get returned a pointer after a failed result retain, Put published replacements before proving ownership and over-retained self-assignment, and shared resize could leak a partially retained copy. | Make element retains status-returning, stage replacement ownership before publication, treat exact self-assignment as refcount-neutral, and roll back every retained slot in failed copy-on-write growth. |
| ZRT-220 | Fixed-WT | P0 | Iterator factories trusted wrong-class source layouts, leaked retained sources/snapshots when adapter construction trapped, advanced cursors before successful result ownership, and ToSeq could return a partial snapshot. | Validate exact sized source identities, transactionally own source/snapshot construction, retain results before cursor movement, and restore the cursor while consuming partial output on every ToSeq failure. |
| ZRT-221 | Fixed-WT | P0 | List scalar and element queries could continue after a failed receiver cast, while Slice/Clone allocated before complete validation and could leak or expose a partial retained result. | Guard every receiver before layout access and implement exact-size Slice/Clone as one all-or-nothing retained-range transaction. |
| ZRT-222 | Fixed-WT | P0 | SortedSet getters ignored failed string-result retains; range/item/set-operation builders accepted invalid inputs or returned partial owning sequences/sets after returning traps. | Validate receivers and string operands first, require successful retained getters, pre-size snapshots, and consume partial range/union/intersection/difference results through checked helpers and recovery cleanup. |
| ZRT-223 | Fixed-WT | P0 | Trie Set/Clone ignored returning value-retain failures, Set's recovery path left the GC mutator scope entered, native traversal-stack growth could silently truncate keys/prefix results, and invalid snapshot receivers became successful empty sequences. | Balance mutator recovery, stage branches/value ownership before publication, reject invalid receivers, transfer copied keys into transactional owning snapshots, and add private parent links so GC tracing, prefix probes, deep cloning, and key reconstruction need no fallible traversal stack. |
| ZRT-224 | Fixed-WT | P0 | `rt_convert_coll.c` allocated destinations before validating source classes, ignored retaining append failures, returned partial conversions, and cloned/popped Stack and Queue through extra native/managed temporary storage; variadic constructors had the same partial-publication problem and `Seq.Of` contradicted its retained-element contract. | Validate exact private source layouts first, borrow stable indexed slots, pre-size Seq/Deque outputs, verify every destination append under local recovery, destroy partial results, stage variadic pointers before fallible construction, and make `Seq.Of` owning. |
| ZRT-225 | Fixed-WT | P0 | Set.Items dereferenced a failed checked cast and ignored result-retain failures, while Bag.Items normalized invalid receivers to empty snapshots and could leak/return partial copied strings. | Validate receivers before allocation, reserve the known cardinality, build snapshots under recovery, move freshly copied Bag strings into the owning Seq, and consume all partial state on returning or nonlocal traps. |
| ZRT-226 | Fixed-WT | P0 | Set length/empty queries and algebra/clone/relation paths dereferenced failed casts or allocated results before validating both operands; union/intersection/difference/clone also ignored failed retains and could return partial Sets. | Guard scalar/traversal storage, validate every nonnull operand before allocation, centralize all-or-nothing Set result construction, choose the smaller intersection/disjoint scan, and propagate retain/allocation failure without publishing a partial result. |
| ZRT-076 | Measure | P2 | `rt_weakmap_len` determines liveness by walking table capacity rather than maintaining an exact live count as weak targets die. | Add a lazily reconciled live/dead counter or generation queue, then benchmark high-capacity mostly-dead maps. |
| ZRT-077 | Open | P1 | WeakMap liveness checks and weak-zero callbacks can interact with growth/compaction and cached entry state. | Specify one synchronization/liveness transition protocol and stress callback, Set, Get, Compact, and finalization concurrently. |
| ZRT-078 | Measure | P2 | WeakMap probing recomputes hashes/string metadata for retained keys during move/match operations. | Cache the key hash and validated byte length in each entry and reuse them during growth and probes. |
| ZRT-079 | Open | P1 | PriorityQueue and comparator-driven sorts can invoke user/runtime comparators after internal mutation has begun. | Stage heap/sort state so a comparator trap leaves the collection valid, and add returning-trap comparator tests. |
| ZRT-080 | Measure | P2 | The shared conversion layer and newly audited snapshots now reserve known source cardinality, but older functional Seq and remaining snapshot helpers still grow incrementally. | Extend checked pre-reservation to the remaining functional/snapshot builders and measure allocation-count reduction before changing less predictable pipelines. |
| ZRT-227 | Fixed-WT | P0 | DefaultMap Remove decremented `count` only after releasing the removed value, while Clear and finalization released callback-capable values from still-published table state; a reentrant value finalizer could drive the count negative, preserve an entry with count zero, or remove/free the outer finalizer's current node twice. | Commit unlink/count changes before release, detach the complete Clear state before callbacks, invalidate finalizing storage up front, and destroy detached native nodes before releasing their values; focused finalizers cover reentrant Clear, Set, and Remove. |
| ZRT-228 | Fixed-WT | P0 | BiMap and CountMap allocated snapshot Seqs before receiver validation and appended copied strings without recovery, so a returning invalid-handle/allocation trap could become a successful empty or partial snapshot while non-local recovery leaked the partial result. | Validate structure first, reserve exact output cardinality, transfer each fresh string into an owning Seq under local recovery, verify every append/count, and consume both managed and native scratch state before propagating failure. |
| ZRT-229 | Fixed-WT | P1 | BiMap Put used the pre-eviction `count + 1` for growth and always allocated/replaced even for an exact mapping; near the load threshold, idempotent or two-sided-conflict updates could needlessly allocate and resize both indexes, and the public signed length boundary was not the insertion cap. | Discover both conflicts first, make exact mappings no-ops, derive growth from the post-conflict count, and reject publication beyond `INT64_MAX`. |
| ZRT-230 | Fixed-WT | P0 | BiMap removal unlinked/freed an authoritative forward entry without first proving the matching inverse link existed, while reverse removal freed the entry even if its forward-chain search failed; any returning invariant trap could leave a dangling index pointer. | Locate both index positions before changing either, remove known conflicts through one two-index helper, and leave the complete mapping untouched when either side is inconsistent. |
| ZRT-231 | Fixed-WT | P1 | CountMap MostCommon cast positive `int64_t n` to `size_t` before bounding it by map size; on 32-bit hosts a request such as 2^32 truncated to zero and returned no keys instead of all available keys. | Compare `n` and cardinality in a wide unsigned domain, cast only when `n` is known smaller, and retain a host-neutral 2^32 boundary regression. |
| ZRT-232 | Fixed-WT | P1 | New-key CountMap IncrementBy/Set attempted a fallible table resize before checking deterministic aggregate-total and public-length overflow. | Reject arithmetic/length overflow before representation growth so impossible updates cannot allocate, rehash, or mask the intended diagnostic with an OOM trap. |
| ZRT-233 | Fixed-WT | P0 | WeakMap construction allocated its managed object before calling a helper that traps internally on native-table OOM; a caller's non-local recovery bypassed the constructor's ordinary null cleanup and leaked the object. | Give construction a local recovery owner, release the partial managed object on every trapped table allocation, preserve the original diagnostic, and publish the finalizer only after table initialization. |
| ZRT-234 | Fixed-WT | P0 | WeakMap probing normalized an invalid retained key to “not equal,” and liveness scans normalized an invalid retained weakref to “dead”; under a returning hook, Set could continue toward a duplicate insertion and growth/compaction could discard corrupt ownership as ordinary garbage. | Use distinct match/probe/liveness error states, validate stored weakref identity before querying or replacement, and stop every lookup/rebuild path immediately after invariant failure. |
| ZRT-235 | Fixed-WT | P0 | WeakMap Remove cleared and transferred each following probe-cluster entry before reinsertion, but a returning rehash trap lost the current entry's owned key/weakref and left a new probe gap. | Restore the exact staged entry and occupied count at its original slot when reinsertion fails; previously moved entries keep the single gap advancing without losing reachability. |
| ZRT-236 | Fixed-WT | P1 | WeakMap Compact returned a dead-entry count from a preliminary scan even though additional weak targets could die before the rebuild, so the reported number could be smaller than the entries actually dropped. | Retain the preliminary scan only as the no-work fast path and count removals from the authoritative rebuild decisions. |
| ZRT-237 | Fixed-WT | P2 | WeakMap IsEmpty delegated to Len and scanned every bucket even after finding a live target. | Validate the receiver directly and return on the first live weakref while preserving invalid-retained-handle diagnostics. |
| ZRT-238 | Fixed-WT | P2 | DefaultMap, BiMap, CountMap, and WeakMap snapshots started with the generic sixteen-slot Seq and grew repeatedly despite knowing an exact count or safe live-count upper bound; copied strings also incurred redundant retain/release pairs. | Allocate owning Seqs at the known bound, transfer freshly constructed strings directly, and keep checked retaining append only where WeakMap must share its stored string handle. |
| ZRT-239 | Measure | P2 | CountMap MostCommon allocates an array for every entry and `qsort`s all counts even when the requested top-N is tiny. | Benchmark an O(N log K), O(K)-space min-heap and an introselect-plus-K-sort path across small/large K, ties, and skewed distributions before choosing one. |
| ZRT-240 | Open | P1 | BiMap and CountMap use public deterministic FNV-1a for attacker-controlled string buckets while DefaultMap, Map, and WeakMap use the runtime's keyed hash; collision sets can force linear chains in both BiMap indexes and CountMap. | Switch both families to `rt_keyed_hash_bytes`, cache hashes where justified, and add adversarial collision/throughput coverage without promising iteration order. |
| ZRT-241 | Measure | P2 | WeakMap's occupied count includes dead targets; reaching 70% always doubles the table even though growth drops dead entries and a same-capacity compaction might restore a low load. | Benchmark dead-density-aware same-capacity rebuild versus direct doubling, including weakref churn and allocation-failure behavior. |
| ZRT-242 | Measure | P2 | Every BiMap insertion performs four native allocations—entry, key bytes, value bytes, and inverse link—before publication. | Benchmark one aligned allocation containing the entry, inverse link, and both byte strings against current fragmentation, removal cost, and cache locality. |
| ZRT-243 | Measure | P2 | Every new CountMap key separately allocates its chain node and key bytes. | Benchmark a checked flexible-layout node/key allocation and measure allocator traffic, lookup locality, and resize behavior. |
| ZRT-244 | Open | P1 | Collection code calls raw `malloc`/`calloc`/`realloc` directly, so DefaultMap/BiMap/CountMap/WeakMap allocation rollback cannot receive deterministic fail-at-N coverage comparable to retained-value fault tests. | Add a zero-dependency internal native-allocation shim with test-only fail scheduling and run constructor, resize, replacement, snapshot, ranking, compaction, and finalizer cleanup sweeps. |
| ZRT-245 | Fixed-WT | P0 | DefaultMap exposed a general callback-reentrancy hazard across TreeMap, OrderedMap, LRUCache, and MultiMap cleanup: retained values could run user finalizers while owner storage remained addressable or while newly reentrant entries fell outside the detached cleanup set. | Each storage shape now commits empty/closed or complete replacement state before callback-capable release, destroys only detached native state, and has Remove/Clear/owner-finalization regressions; ZRT-263/ZRT-264 track the narrower remaining all-family trap/resurrection sweep. |
| ZRT-246 | Fixed-WT | P0 | Map and IntMap Remove released the detached value before decrementing count, Clear released one chain at a time before publishing an empty table, and owner finalizers invoked public Clear while the zero-refcount object remained fully addressable; reentrant value finalizers could make counts negative, preserve uncounted entries, or double-free the outer node. | Decrement/unlink before release, detach every chain before Clear callbacks, invalidate owner storage before finalization cleanup, reclaim native nodes before releasing values, and cover reentrant Clear, Set, and Remove for both key shapes. |
| ZRT-247 | Fixed-WT | P0 | TreeMap replacement, Remove, Clear, and owner finalization released retained values while array slots or the complete owner array still exposed the pre-callback state; a value finalizer could clear/reinsert into storage later shifted, zeroed, or freed by the outer operation. | Publish replacement/removal mutations before release, detach the complete array before Clear/finalizer callbacks, block mutation only while the zero-refcount owner is finalizing, and preserve callback-created state plus ordinary Clear capacity with pointer-and-generation regressions. |
| ZRT-248 | Fixed-WT | P0 | OrderedMap Remove decremented `count` after releasing the detached value, so a value finalizer that cleared the map made the outer decrement publish `-1`; owner finalization also left a zeroed but writable bucket table available to callback insertions that were orphaned when the outer finalizer freed it. | Commit count/list/bucket removal before release, detach the complete table during owner finalization, block finalizer-time mutation, and preserve ordinary Clear callback insertions with focused regressions. |
| ZRT-249 | Fixed-WT | P1 | OrderedMap Clear/finalization stopped at the first trapping value finalizer, leaking the remaining native nodes, keys, and retained values. | Reclaim each native node before releasing its value, resume at the next detached node after a non-local trap, finish all cleanup, and then rethrow the first diagnostic. |
| ZRT-250 | Fixed-WT | P1 | OrderedMap key/value snapshots used default-capacity Seqs and treated a returning failed `Seq.Push` as success, which could publish a short snapshot; fresh copied keys also incurred a redundant retain/release pair. | Validate before allocation, reserve the exact cardinality, transfer fresh strings directly, and require each void append to advance length by exactly one before continuing. |
| ZRT-251 | Fixed-WT | P1 | TreeMap owner teardown freed its detached array but neither drained past a trapping value finalizer nor reinstalled its one-shot finalizer when a contained value resurrected the owner; a reused resurrected map could later leak its native array. | Drain every detached slot while preserving the first trap, leave resurrection as a valid empty lazy map, and re-arm the TreeMap finalizer before returning a resurrected owner. |
| ZRT-252 | Fixed-WT | P0 | LRUCache replacement published the new value, released the old value, and only then promoted the node; an old-value finalizer that cleared the cache freed the node before the outer promotion dereferenced it. | Retain and publish the replacement, complete MRU promotion, leave the mutator transaction, and only then release the displaced value. |
| ZRT-253 | Fixed-WT | P0 | LRUCache eviction released the victim before inserting the prepared replacement; at capacity one, a victim finalizer could insert another key and the outer Put then published a second resident entry, violating the fixed cap. | Detach the victim and publish the replacement as one complete mutation, then release the detached victim so reentrant operations observe and may supersede the committed cache state. |
| ZRT-254 | Fixed-WT | P0 | LRUCache owner finalization left its bucket table writable during value callbacks, while Clear/finalization stopped on the first trapping finalizer and native nodes remained allocated until after callback release. | Detach/close owner storage before finalizer callbacks, reclaim native nodes first, drain all detached nodes after traps, preserve reentrant ordinary Clear mutations, and re-arm a resurrected owner. |
| ZRT-255 | Fixed-WT | P1 | `rt_lrucache_new(capacity)` sized the empty hash table for the full eviction limit, so a large logical limit attempted gigabytes or overflowed before the cache held one entry. | Treat capacity solely as an eviction limit: start with sixteen buckets and grow from actual resident count, with an `INT64_MAX`-limit constructor regression on 64-bit hosts. |
| ZRT-256 | Fixed-WT | P1 | LRUCache used deterministic FNV-1a for attacker-controlled keys, allowing precomputed collision sets to turn lookup, insertion, and eviction bookkeeping into long chain walks. | Use the runtime's per-process keyed byte hash for lookup, insertion, removal, and rehash without changing observable MRU order. |
| ZRT-257 | Fixed-WT | P1 | LRUCache snapshots could normalize a returning append failure to a successful short Seq and always began at the generic sixteen-slot capacity. | Reserve the exact resident count, transfer freshly copied keys, retain values transactionally, and verify every append's length postcondition. |
| ZRT-258 | Fixed-WT | P0 | MultiMap Clear detached and destroyed one bucket at a time but reset `key_count`/`total_count` only afterward; a value finalizer could insert into an already-visited bucket and leave an uncounted entry, or append into a future bucket that the outer Clear then destroyed. | Rewrite every old collision link into one detached list and publish zero buckets/counts before any Seq release, so callback-created entries remain independent and counted. |
| ZRT-259 | Fixed-WT | P0 | MultiMap owner finalization called public Clear while the bucket table remained usable, did not re-arm after resurrection, and could abandon later per-key Seqs when one value finalizer trapped. | Close/detach the complete owner table, reclaim entry/key nodes before Seq release, drain after traps, restore an empty table and one-shot finalizer only on resurrection, then propagate the first diagnostic. |
| ZRT-260 | Fixed-WT | P0 | MultiMap's staged-entry, existing-key, Get-snapshot, and key-snapshot helpers assumed void `Seq.Push` calls succeeded when a configured trap hook returned; metadata could advance or a partial snapshot could be published without the element. | Record the pre-append length and require an exact `+1` postcondition in every helper, consuming staged ownership or the partial snapshot on mismatch. |
| ZRT-261 | Fixed-WT | P1 | MultiMap used deterministic FNV-1a for externally supplied keys, making its separate chains susceptible to precomputed collision denial of service. | Use `rt_keyed_hash_bytes` consistently for lookup, insertion, removal, and resize rehashing. |
| ZRT-262 | Fixed-WT | P2 | Every new MultiMap key allocated the default sixteen-slot backing Seq for its first value, and Get/Keys snapshots repeated default-capacity growth despite exact lengths. | Start each private value Seq at one slot and reserve exact value/key snapshot cardinality, retaining growth only for genuinely repeated values. |
| ZRT-263 | Open | P1 | DefaultMap, Map, IntMap, and additional value-owning containers still release detached elements in straight loops; one non-local value-finalizer trap can skip later releases and native cleanup even though OrderedMap, TreeMap, LRUCache, and MultiMap now drain and rethrow. | Introduce a shared or consistently patterned drain-first-error helper, advance ownership before each callback, and add first/middle/last trapping-finalizer regressions across every remaining owning storage shape. |
| ZRT-264 | Open | P1 | Collection finalizers are one-shot, but the remaining owning families have not all been tested for a contained value resurrecting the zero-refcount owner; without explicit re-arming, subsequent reuse and release can leak newly allocated native storage. | Audit every owner finalizer, define the valid empty resurrected state, reinstall cleanup only when the owner refcount became nonzero, and test reuse plus a second final release. |

## Files, streams, SaveData, watchers, archives, and compression

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-081 | Fixed-WT | P0 | `archive_open_parent_for_file_posix` accepted null `name`/`out_leaf`, then used them while splitting a normalized entry. | Reject either null argument, clear the required output before fallible work, and return immediately after the trap. |
| ZRT-082 | Fixed-WT | P0 | `savedata_require` checked only a class identifier through a direct object read and callers often dereferenced its result after a returning trap. | Use sized `rt_obj_is_instance` validation and make every caller branch on the returned pointer. |
| ZRT-083 | Fixed-WT | P0 | SaveData key/value helpers could return sentinel text after trapping, allowing Set/Get/Remove to continue with invalid string handles. | Distinguish null/invalid strings, return status/null, and stop before any length, UTF-8, or entry-list access. |
| ZRT-084 | Fixed-WT | P0 | New SaveData string entries retained key then value; a returning second retain left ambiguous partial ownership. | Make pair retain status-returning, initialize outputs, roll back either reference, and require success before node allocation. |
| ZRT-085 | Fixed-WT | P1 | POSIX/Windows `fdopen` failure cleanup overwrote the original conversion error with the descriptor-close result. | Preserve `errno` across cleanup so callers receive the actual stream-construction failure. |
| ZRT-086 | Fixed-WT | P0 | `ensure_parent_dir` created its runtime string outside the recovery scope and unconditionally unreferenced it. | Bring allocation under local recovery, keep the reference volatile across `setjmp`, and release only when publication succeeded. |
| ZRT-087 | Fixed-WT | P1 | SaveData setters ignored allocation/retain failure from entry helpers and could report success through normal return. | Propagate helper status and raise a deterministic allocation failure without mutating the old entry. |
| ZRT-088 | Fixed-WT | P1 | SaveData Load did not require an exact `fread` result and size validation did not cover the allocation terminator arithmetic explicitly. | Reject empty/unrepresentable sizes, allocate checked `size + 1`, and fail transactionally on every short read. |
| ZRT-089 | Fixed-WT | P0 | Zstd FSE table construction did not prove normalized counts exactly filled the active table before cells were consumed. | Validate signed counts and checked aggregate equality, then clear the active cells before distribution. |
| ZRT-090 | Fixed-WT | P0 | Zstd Huffman construction accepted a derived zero-bit table and had incomplete bounds on the maximum bit width. | Require `1 <= max_bits <= ZSTD_MAX_HUF_BITS` before generating decode cells. |
| ZRT-091 | Fixed-WT | P0 | Zstd parsing repeatedly used `offset + length > capacity`, which can wrap before the comparison on hostile frames. | Replace additions with `used <= capacity && add <= capacity - used` throughout literals, sequences, blocks, headers, and checksum parsing. |
| ZRT-092 | Fixed-WT | P0 | Zstd zero-length decode paths still formed output pointers or called `memcpy`/`memset` on possibly null storage. | Guard zero-length copies/fills and avoid pointer arithmetic unless at least one byte is produced. |
| ZRT-093 | Open | P1 | SaveData Load accepts any file size below `SIZE_MAX`, then allocates that entire size plus a runtime-string copy. | Introduce configurable default/hard byte ceilings, checked before allocation, with boundary and sparse-file tests. |
| ZRT-094 | Measure | P2 | SaveData stores entries in a singly linked list; Set/Get/Has/Remove are O(number of keys). | Add a string-hash index while retaining a stable serialization list, and benchmark realistic save cardinalities. |
| ZRT-095 | Fixed-WT | P2 | `rt_savedata_count` walked the complete entry list on every call. | SaveData now maintains a checked exact count transactionally across insert, replacement, remove, clear, missing-file load, failed load, and successful load; a 2,048-key mixed-mutation regression protects cardinality. |
| ZRT-096 | Open | P2 | SaveData serializes current linked-list order, which depends on mutation/load history and makes equivalent states byte-different. | Define deterministic bytewise key ordering for persistence and add reproducible-output tests. |
| ZRT-097 | Measure | P2 | SaveData Save builds the complete JSON document in `rt_string_builder` before the atomic writer copies it to a staged file. | Add a streaming staged-file JSON encoder with the same atomic replacement and trap rollback guarantees. |
| ZRT-098 | Measure | P2 | SaveData Load copies file bytes into a native buffer, then copies again into a runtime string before streaming tokenization. | Let the JSON stream parser consume a bounded borrowed byte span or incremental file source, subject to an ADR if its public surface changes. |
| ZRT-099 | Open | P1 | Repeated JSON object keys are silently resolved by repeated `savedata_set_*_entry` replacement. | Specify reject-versus-last-wins behavior explicitly and add duplicate-key tests; rejecting is safer for corrupted save detection. |
| ZRT-100 | Open | P1 | SaveData has no independent limits for entry count, key length, value length, or total decoded string bytes. | Add checked parser budgets so many tiny entries or one huge decoded value cannot exhaust memory within the file-byte ceiling. |
| ZRT-101 | ADR | P1 | SaveData has no synchronization or generation check; concurrent Set/Save/Load can traverse and replace the same linked list. | Define thread-safety in an ADR, then use snapshots/versioning or a per-object lock with I/O outside the lock. |
| ZRT-102 | Open | P1 | Atomic Save can complete rename but fail the final parent-directory durability step, leaving the Boolean result unable to say whether the new file is already visible. | Document the commit point, preserve a distinct internal “committed but durability uncertain” result, and add syscall fault-injection coverage. |
| ZRT-103 | Fixed-WT | P1 | `rt_file_write` returned success for `len == 0` before validating the `RtFile` descriptor, unlike non-empty writes. | Validate the handle first so a closed/invalid channel cannot be reported as a successful operation merely because the payload is empty. |
| ZRT-104 | Fixed-WT | P1 | `rt_binfile_write` made one `fwrite` call and trapped on a short result without retrying a recoverable partial write. | Loop until complete or `ferror`, with checked addressability and defensive zero-progress termination. |
| ZRT-105 | Open | P1 | `rt_binfile_size` temporarily seeks; when end-position acquisition or restoration fails, the original logical position/state may be lost and one restore failure is ignored. | Centralize save/restore cleanup, report both primary and restoration failure, and invalidate position state if restoration cannot be guaranteed. |
| ZRT-106 | ADR | P2 | `rt_binfile_pos` and `rt_binfile_size` call `binfile_prepare_seek`, which flushes pending output, so nominal queries have I/O/error side effects. | Specify this behavior or separate logical buffered position from durability-changing flush in an ADR. |
| ZRT-107 | ADR | P2 | `rt_memstream_clear` intentionally retains peak capacity and there is no Trim/Shrink operation for one-off large buffers. | Add an explicit trim API with a checked target-capacity contract and document whether cursor/length can constrain shrinking. |
| ZRT-108 | Open | P1 | MemStream permits seeking to any nonnegative `int64_t`; the next small write allocates and zero-fills the entire sparse gap. | Add a configurable stream byte ceiling and reject writes whose resulting length exceeds it before allocation/zero fill. |
| ZRT-109 | Fixed-WT | P1 | MemStream float encoders assumed 32-bit IEEE binary32 and 64-bit IEEE binary64 representations while copying their object representations into fixed-width integers. | Enforce 8-bit bytes plus the binary radix, precision, exponent range, and exact storage width for binary32/binary64 with explicit compile-time diagnostics on unsupported targets. |
| ZRT-110 | Measure | P2 | File-backed ZPAK reads serialize seek/read through one `zpak_read_lock_t`, while each entry has an independent validated offset/range. | Use descriptor-offset reads (`pread` or the Windows overlapped equivalent in adapters) to permit parallel entry reads without shared file-position locking. |

## Networking, HTTP, TLS, SSE, WebSocket, and HTTP/2

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-111 | Fixed-WT | P0 | TLS `send_record` accepted a null session or an invalid socket far enough to enter record construction/native send. | Reject both states before allocation or field access and return typed invalid/closed results. |
| ZRT-112 | Fixed-WT | P0 | TLS `recv_record` did not initialize outputs on all errors, and graceful disposal could attempt close-notify on an invalid descriptor. | Validate every pointer/socket, zero outputs first, and gate graceful network I/O on a live socket. |
| ZRT-113 | Fixed-WT | P0 | SSE transport helpers could dereference a null receiver/data pointer or operate on `INVALID_SOCK`; several state assignments were overwritten before observation. | Validate receiver/data/length/socket at the transport boundary and remove dead stores that obscured the state machine. |
| ZRT-114 | Fixed-WT | P0 | HTTP client default-header application allocated, retained, and called request setters while holding partial snapshots; returning traps leaked or continued. | Make `apply_defaults` status-returning with transactional key/value snapshots, lock cleanup, and immediate caller exit. |
| ZRT-115 | Fixed-WT | P0 | Cookie request-header construction had fallible URL accessors, native growth, and managed header publication without one recovery owner. | Give `apply_cookie_header` a recovery state that owns URL parts, native header, mutex state, and managed values until publication. |
| ZRT-116 | Fixed-WT | P0 | Response-cookie parsing could leave staged cookie mutations/resources after accessor, header, allocation, or commit traps. | Stage the complete response cookie transaction and commit only after all fallible parsing/ownership steps succeed. |
| ZRT-117 | Fixed-WT | P0 | `do_request` assumed traps never return and continued across request clone, defaults, cookie, send, response, and redirect operations. | Enclose the redirect transaction in local recovery, check every status/result, and release request/response/URL state on one failure path. |
| ZRT-118 | Fixed-WT | P0 | HTTP client cookie snapshotting trusted allocation/count results and could publish a partial Map after a returning trap. | Validate snapshot counts, stage native copies, and consume every partial managed/native result before returning null. |
| ZRT-119 | Fixed-WT | P0 | `rt_http_server_new` could publish a partially initialized server when object, mutex, or router setup trapped and returned. | Track initialized components, destroy only those components, and release the partial server before returning. |
| ZRT-120 | Fixed-WT | P0 | `rt_server_res_json` modified response state through fallible body/header cloning and insertion without preserving the prior response on failure. | Build body and replacement headers off-object, verify each insertion, then swap the completed response transactionally. |
| ZRT-121 | Fixed-WT | P0 | Multipart Build could copy into a missing/invalid Bytes allocation after a returning trap. | Check result and writable storage before `memcpy`, with local recovery releasing the partial result. |
| ZRT-122 | Fixed-WT | P1 | Generic ConnectionPool performed `select`/`MSG_PEEK` health checks while holding its bookkeeping mutex. | Reserve and retain a matching TCP under lock, probe outside it, and safely remove an unhealthy reservation after reacquiring. |
| ZRT-123 | Fixed-WT | P1 | HTTP ConnectionPool health probes and transport closure ran under the pool mutex, creating a readiness/close convoy. | Detach the candidate under lock and do health/close work outside, with slot validation on reentry. |
| ZRT-124 | Fixed-WT | P0 | A concurrent HTTP pool Clear could be followed by release of an older checked-out lease, repopulating the cleared pool. | Increment a pool generation on Clear and only return a connection when its captured generation still matches. |
| ZRT-125 | Fixed-WT | P1 | HTTP pool release allocated an origin key and probed transport health in lock-sensitive code. | Precompute/probe outside the mutex and keep the locked section to validated slot/key publication only. |
| ZRT-126 | Fixed-WT | P0 | HTTP response/status parsing read incompletely initialized line storage and insufficiently bounded status fields; `has_header` assumed every entry name was non-null. | Zero-initialize line buffers, initialize status outputs, require minimum syntax/length, and skip malformed null-name entries. |
| ZRT-127 | Fixed-WT | P0 | URL query decoding could release its output Map and then continue inserting when string/value construction trapped and returned. | Stop and return null after releasing the partial Map on any key/value/decode publication failure. |
| ZRT-128 | Fixed-WT | P0 | HTTP/2 I/O callbacks could claim more bytes than requested or claim success without initializing the destination; frame parsing then consumed invalid bytes. | Reject oversized callback counts and use deterministic initialized frame storage, with missing/oversized/unwritten callback regressions. |
| ZRT-129 | Fixed-WT | P1 | HTTP request timeout was installed as a per-socket-operation timeout, so a peer could continuously trickle bytes and keep the full response alive indefinitely. | Public request/download entry now creates one saturating monotonic deadline carried through connection setup, TLS records, HTTP/1 and HTTP/2 I/O, redirects, and response transforms; focused body, redirect, and TLS-trickle regressions enforce it. |
| ZRT-130 | Fixed-WT | P1 | Plain HTTP `http_create_tcp_socket` applied `timeout_ms` independently to each resolved address, unlike the TLS/SSE shared-budget paths. | Resolution and every address candidate now share the request's absolute deadline, with a freshly rounded remaining budget before each connection attempt. |
| ZRT-131 | Fixed-WT | P1 | HTTP gzip decoding enforced its 256 MiB ceiling only after managed gunzip output existed, copied the payload through managed/native intermediates, and had no expansion-ratio budget. | The ADR-0229 native decoder now bounds aggregate growth by an absolute limit plus caller-selected ratio/slack, adopts the first member allocation, and lets HTTP decode directly under 256 MiB and 128:1 plus 1 MiB limits; focused native and loopback bomb regressions cover the policy. |
| ZRT-132 | Measure | P2 | HTTP pool capacity is one fixed array shared by all origins; one high-cardinality origin set can evict or occupy every slot. | Add per-origin idle quotas/fair eviction and benchmark mixed-origin workloads. |
| ZRT-133 | Fixed-WT | P1 | `http_conn_pool_evict_idle_locked` reset expired entries while holding the mutex, and reset could cascade into TLS/socket teardown; generic pool expiry and clear/release paths similarly closed or released TCP under lock. | HTTP and generic pools now move compact ownership records under the bookkeeping mutex, preserve exclusive close claims across unlock, and perform transport teardown plus managed-reference release afterward. |
| ZRT-134 | Measure | P2 | HTTP request sending serializes headers plus the complete body into one contiguous `request_buf` before transport send. | Send headers and body as checked segments, or use adapter scatter/gather where available, to avoid body-sized duplication. |
| ZRT-135 | Fixed-WT | P1 | Streaming HTTP download previously risked exposing partial destination content after an error, redirect failure, or length mismatch. | The public path-based helper exclusively creates a randomized same-directory sibling, streams and syncs it, preserves ordinary replacement permissions, atomically renames it, and removes staged content on every pre-publication failure; focused download tests cover success and trapped cleanup. |
| ZRT-136 | Measure | P2 | TLS `send_record` allocates and securely clears maximum-sized record and plaintext buffers per record; `recv_record` allocates each payload. | Move bounded record scratch buffers into the session or a secure reusable arena and benchmark throughput/latency. |
| ZRT-137 | Open | P3 | Many TLS socket/protocol failures collapse to static strings such as “send failed” or “decryption failed,” losing native/protocol context. | Store structured error category, alert, handshake state, and native code while preserving the compatibility string accessor. |
| ZRT-138 | Open | P1 | The from-scratch TLS handshake/record parser has broad state and hostile-input exposure but limited differential transcript fuzzing. | Add deterministic record/handshake fuzz targets and compare accepted/rejected transcripts against independent RFC test corpora without product dependencies. |
| ZRT-139 | Measure | P2 | Certificate parsing/path verification is repeated for new sessions to the same endpoint/trust generation. | Cache only verified immutable chain results keyed by hostname, chain fingerprint, policy, and trust-store generation, with bounded expiry. |
| ZRT-140 | Open | P2 | SSE reconnect uses server retry delay but does not add randomized jitter, allowing many clients to reconnect simultaneously after an outage. | Apply bounded jitter and exponential failure backoff while respecting server-provided minimums and the caller's overall deadline. |
| ZRT-141 | Measure | P2 | SSE raw and event line readers allocate/reallocate/free native buffers for successive lines on a long-lived stream. | Retain bounded reusable line/event buffers in the SSE object and trim pathological peaks after dispatch. |
| ZRT-142 | Open | P1 | WebSocket broadcast snapshots clients but sends to them sequentially; one slow client's blocking send delays every later target despite the source comment claiming otherwise. | Queue per-client outbound frames or impose a strict per-client broadcast deadline, then continue other clients independently. |
| ZRT-143 | ADR | P2 | HTTP/2 connection operations synchronously service one request/stream flow and do not provide a dispatcher for concurrent stream multiplexing. | Specify connection/stream concurrency and cancellation in an ADR, then add a reader dispatcher and per-stream state queues. |
| ZRT-144 | Measure | P2 | HPACK dynamic-table lookup is linear, insertion memmoves the entire entry array, and each name/value is separately allocated. | Use a bounded ring for RFC index order plus hash indexes/arena storage; benchmark realistic header reuse. |
| ZRT-145 | Open | P1 | HTTP server accept/worker paths have fixed capacity but limited explicit overload policy and observability for queue rejection, slow clients, and saturation. | Define backpressure/rejection behavior and expose accepted, active, queued, rejected, timeout, and parse-limit counters. |

## Audio and localization

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-146 | Fixed-WT | P1 | Every audio FX Add allocated an effect before `append_fx` rejected an invalid group, leaking the node and any delay/reverb buffers. | Validate the group before allocation and make `append_fx` consume/free the node on every failure path. |
| ZRT-147 | Fixed-WT | P1 | `g_next_fx_id++` could invoke signed overflow and wrap to an identifier still owned by a live effect. | Advance without overflowing, mark wrap, and scan for an unused positive identifier before reuse. |
| ZRT-148 | Fixed-WT | P0 | Locale constructors assumed OOM traps never return and dereferenced null from `loc_alloc`. | Check allocation in invariant, canonical-tag, parse, and from-parts constructors; add returning-trap allocation regressions. |
| ZRT-149 | Open | P1 | Audio FX processing holds `g_fx_lock` through the complete DSP block; control operations spin, while mixer contention bypasses the entire effect chain for that block. | Publish immutable/refcounted effect-chain snapshots or use an audio-epoch handoff so the callback never locks or audibly drops processing. |
| ZRT-150 | Open | P1 | `rt_audio_fx_process_group` ignores `sample_rate`; biquad, delay, and reverb state is designed at a fixed 44.1 kHz. | Store design parameters and rebuild coefficients/delay lengths when the backend rate changes; test 44.1, 48, 96, and 192 kHz. |
| ZRT-151 | Measure | P2 | `mp3_file_to_wav` reads the complete encoded file into memory before decoding, in addition to decoder PCM and final WAV storage. | Feed the decoder from a bounded file reader and avoid the encoded-file-sized allocation; retain the 256 MiB source ceiling. |
| ZRT-152 | Fixed-WT | P1 | Ogg `packet_append` was overflow-checked but had no packet-byte ceiling, and the stream-state/queued-packet lists had no count/aggregate budget. | The reader now enforces 64 MiB per-packet and aggregate partial-buffer ceilings, at most 64 active logical streams, and explicit format-derived ready-queue count/byte ceilings before allocation. |
| ZRT-153 | Measure | P2 | Ogg/MP3 decoding accumulates a complete PCM buffer, then `build_wav_from_pcm` allocates and copies a second full audio payload. | Allocate the final WAV once when size is known, or stream PCM into a growable WAV payload with one bounded backing buffer. |
| ZRT-154 | Measure | P2 | MusicGen renders a full 32-bit stereo accumulator, converts it into a second full 16-bit PCM buffer, then copies that into WAV output. | Down-convert/soft-clip directly into the final WAV allocation after accumulation, eliminating the intermediate PCM buffer. |
| ZRT-155 | Measure | P2 | LocaleManager lookup, initialization matching, register, and unregister paths linearly scan `g_mgr.entries` by canonical tag. | Add a hash index keyed by canonical tag while preserving the registry array for deterministic enumeration. |
| ZRT-156 | Measure | P2 | `tz_rule_at` linearly walks every sorted transition from the beginning for each timestamp query. | Use upper-bound binary search and cache the last transition index for nearby sequential formatting. |
| ZRT-157 | ADR | P0 | MessageBundle `as_bundle` is an unchecked cast and `bundle_alloc` performs fallible locale/map retains after allocation without complete rollback. | Give localization object kinds stable class IDs in an ADR, validate sized handles, and make constructor/fallback publication transactional. |
| ZRT-158 | ADR | P0 | ListFormat, RelativeTimeFormat, NumberFormat, DateFormat, and related classes also use unchecked `as_fmt` casts before field access. | Introduce shared class-specific validators and forged-handle/returning-trap tests for every public formatter entry point. |
| ZRT-159 | Measure | P2 | Each MessageBundle locale fallback lookup rebuilds the Locale fallback list and repeatedly allocates prefixed `tag:key` strings. | Cache the immutable fallback-tag chain per Locale and probe with stack/borrowed composite keys or a two-level map. |
| ZRT-160 | Fixed-WT | P0 | `rt_locale_manager_retain_data` used unchecked signed `fetch_add`; refcount exhaustion wrapped to a negative value before release-side checks could protect the record. | Retain/release now use checked CAS loops, trap without modifying invalid boundaries, and treat `INT64_MAX` as a fail-safe pinned state when a void-API embedder trap returns; maximum-boundary regressions cover both operations. |
| ZRT-161 | Fixed-WT | P0 | `ogg_reader_next_packet_ex` freed the prior reader-owned packet before parsing, but failure left `out_data`, `out_len`, and metadata unchanged; callers could retain a dangling packet pointer. | Initialize every supplied output before validation/parsing so EOF, malformed input, invalid arguments, allocation failure, and resource-limit rejection deterministically publish null/zero state. |
| ZRT-162 | Fixed-WT | P1 | Crypto policy accepted every unknown mode as compatibility mode and every unknown service identifier while in compatibility mode, so malformed/version-skewed values failed open. | ADR 0231 makes both enum boundaries strict: unknown modes are rejected without state mutation and unknown services are denied in every mode, with focused policy-state regressions. |

## GUI, input, crypto follow-up, and common utilities

| ID | State | Pri | Source evidence | Recommendation |
|---|---|---:|---|---|
| ZRT-163 | Fixed-WT | P0 | A synthesized `CLICK`, `DOUBLE_CLICK`, or `TRIPLE_CLICK` handler could destroy its target, after which `vg_event_send` dereferenced the dead pointer while recording click history; pointer-only liveness checks also admitted an address-reused replacement. | Carry the target's widget ID across callbacks, stop later synthesis/history work unless pointer and ID still match, and apply the same generation check while bubbling. |
| ZRT-164 | Fixed-WT | P0 | Mouse frame finalization subtracted arbitrary signed 64-bit coordinates directly, so moving between extreme values invoked signed-overflow undefined behavior. | Use saturating signed subtraction for both begin-frame and post-poll finalization, with endpoint regressions in both directions. |
| ZRT-165 | Fixed-WT | P0 | Raw-delta and wheel paths converted NaN, infinity, and out-of-range doubles to `int64_t`, and wheel accumulation could become non-finite; those conversions are undefined or implementation-dependent. | Reject non-finite deltas, keep wheel accumulation finite, and round/truncate through explicit saturating conversion helpers. |
| ZRT-166 | Fixed-WT | P1 | Mouse double-click bookkeeping initialized the prior-click timestamp to zero and accepted `now - 0 <= 400`, so the first click early in process lifetime could be reported as a double-click. | Use an explicit no-prior-click sentinel and require ordered, nonnegative timestamps before checked interval subtraction. |
| ZRT-167 | Fixed-WT | P0 | A TextInput `on_change`/`on_commit` callback could destroy and replace its widget while insert history, undo/redo, composition commit, clipboard shortcuts, or key dispatch continued through the stale address. | Capture the base widget ID before callback-capable operations, treat a destroyed target as a completed edit, and stop all post-callback history/paint/composition access unless pointer and ID still match. |
| ZRT-168 | Fixed-WT | P0 | `run_process` passed length-aware C++ strings to NUL-terminated native argv, cwd, and environment APIs without rejecting embedded NUL, silently launching with attacker-controlled truncation. | Validate every argument, optional working directory, environment name, and environment value before any filesystem lookup or launch; return a deterministic launch failure on embedded NUL. |
| ZRT-169 | Fixed-WT | P1 | Ogg lacing permits a zero-length packet, but `queue_completed_packet` rejected `partial.len == 0`; the page walker also formed `body + 0` when the zero-body pointer was null. | Accept empty packets with a reader-owned nonnull one-byte sentinel allocation, avoid null pointer arithmetic, preserve metadata/container order, and cover an empty packet followed by another logical stream. |
| ZRT-170 | Fixed-WT | P1 | The Ogg reader stored `page_sequence` but did not validate per-serial sequence continuity, duplicate pages, or a missing continuation between partial packets, allowing corrupt bytes to be spliced into one logical packet. | Track the next sequence independently per serial, reject gaps/replays and missing continuation flags before appending, reset the partial on failure, and verify modulo-2^32 wraparound. |
| ZRT-171 | Fixed-WT | P0 | ChaCha20-Poly1305 and both AES-GCM encryptors addressed/wrote the tag at `plaintext_len + 16` without first proving that sum fit `size_t`; algorithm caps exceed 32-bit address space. | All three encryptors now reject `plaintext_len > SIZE_MAX - 16` before output access, with maximum-length regressions that are safe on both 32- and 64-bit targets. |
| ZRT-172 | Fixed-WT | P1 | ChaCha20-Poly1305 cleared the derived one-time key but left the key-derived `poly1305_ctx`, computed tag, and length block in stack storage after encrypt/decrypt. | Encrypt and decrypt now securely clear MAC context, length encoding, computed tag, comparison residue, and derived key on success and authentication/error exits. |
| ZRT-173 | Fixed-WT | P0 | SHA finalizers implemented padding through the ordinary message-update path, so a near-`UINT64_MAX` SHA-256 context trapped under padding growth and then emitted a digest from incomplete state; one-shot SHA-256 likewise finalized after an oversized update trap returned. | Compress SHA-256/384/512 final padding directly without advancing the logical count, reject oversized one-shot SHA-256 input before initialization/finalization, and cover the forged-count returning-trap boundary while retaining all SHA/HMAC vectors. |
| ZRT-174 | Measure | P2 | HKDF-SHA-384 allocates, copies, wipes, and frees `T(n-1) || info || n` for every expansion block while the SHA-256 implementation streams its HMAC input. | Add incremental HMAC-SHA-384 updates and benchmark allocation-free expansion for large `info` and maximum output. |
| ZRT-175 | ADR | P1 | `rt_input.c` claims one GC-managed context per Canvas, but keyboard and mouse level/edge/text/click state is process-global; binding another canvas therefore shares or overwrites interaction state. | Specify multi-canvas input ownership in an ADR, then move snapshots into per-canvas contexts or explicitly constrain and enforce a single active input canvas. |
| ZRT-176 | Fixed-WT | P1 | Keyboard pressed/released vectors could grow to `INT_MAX` entries through repeated same-frame transitions and retain their peak capacity indefinitely, despite their public snapshot semantics being set-like. | Deduplicate each key through the existing frame bitsets and store first-edge order in fixed `ZANNA_KEY_MAX` arrays, making the bound structural and removing reallocations from the input-event hot path. |
| ZRT-177 | Open | P1 | The per-frame keyboard text buffer has only an `INT_MAX` ceiling and also retains peak capacity, allowing a synthetic/hostile event burst to drive large persistent allocation. | Add a configurable UTF-8 byte budget, preserve whole codepoints on rejection, and trim excess capacity after a bounded cold period. |
| ZRT-178 | ADR | P1 | Runtime mouse click/double-click detection uses only button and elapsed time; it ignores press/release distance, prior-click distance, and canvas identity, so quick drags or clicks in different windows qualify. | Define spatial and canvas-scoping semantics in an ADR, store press/click positions and canvas generations, and add HiDPI-aware threshold tests. |
| ZRT-179 | Fixed-WT | P2 | Global input test-hook setters were callable without the main-thread assertion used by surrounding state and could race platform query/warp callbacks if linked outside tests. | Apply the input subsystem's existing main-thread ownership assertion to both hook setters and the reset operation so publication and consumption share one enforced execution domain. |
| ZRT-180 | Measure | P2 | On X11 without a bound canvas, every Caps Lock query may `XOpenDisplay`, query, and `XCloseDisplay`, introducing connection setup and possible display-server latency into polling. | Cache state from keyboard events or a safely owned display connection and benchmark polling with and without a canvas. |
| ZRT-181 | Measure | P2 | GUI per-root event state is found by linearly scanning a capacity-doubled sparse slot array on activation, save, prune, and subtree-forget operations. | Track live slots/count, compact tombstone-heavy storage, or index roots by pointer plus widget ID after measuring multi-window dispatch. |
| ZRT-182 | Fixed-WT | P0 | Capture, hover, focus, modal, and hit-test dispatch contained callback boundaries followed by pointer-only `vg_widget_is_live` checks; address reuse could route the remainder into an unrelated replacement widget. | Carry pointer-plus-ID references across dispatch, hover, capture, modal, root, and focus callbacks; detach old focus before notification, validate both generations afterward, and preserve a reentrant callback's focus decision. |
| ZRT-183 | Open | P2 | GUI ancestry/subtree and focus walks trust parent chains to be acyclic; metadata corruption or a reentrant custom widget that creates a cycle can hang dispatch/destruction. | Reject cycle-forming reparent operations, assert the invariant in debug builds, and bound defensive release walks by the live-widget count. |
| ZRT-184 | Open | P1 | TextInput stores lengths in `size_t` but calls UTF-8/font helpers through `int` offsets and caches `vg_utf8_strlen`'s `int` result, so content above `INT_MAX` cannot be represented coherently. | Establish an explicit text byte/grapheme ceiling below `INT_MAX` or migrate the complete UTF/font indexing path to `size_t`, with boundary tests. |
| ZRT-185 | Measure | P2 | Every TextInput edit recomputes undo bytes by `strlen`-scanning every retained full-text snapshot until the 64 KiB budget is met. | Store each snapshot length and maintain a checked aggregate counter so history eviction is O(number evicted), not O(history bytes) per edit. |
| ZRT-186 | Fixed-WT | P1 | TextInput's 64 KiB undo budget evicted only while more than one snapshot existed, so one current snapshot larger than the budget remained retained in addition to the live text. | Refuse snapshots whose text plus terminator cannot fit the budget, clear prior history when an edit crosses that ceiling, and keep undo/redo disabled until a later state can establish a bounded baseline. |
| ZRT-187 | Open | P1 | TextInput callbacks may synchronously perform another edit; outer `insert_text` history publication then observes the reentrant final state and can duplicate or misorder undo snapshots. | Commit history before notification or add a mutation generation/reentrancy transaction that coalesces nested edits deterministically. |
| ZRT-188 | ADR | P2 | Password-mode TextInput masks rendering but retains ordinary selection/copy APIs, so secrets can be copied to the process-global clipboard without an explicit policy. | Specify password clipboard/selection behavior and add an opt-in policy surface rather than relying on presentation masking as protection. |
| ZRT-189 | ADR | P1 | `run_process` captures unlimited stdout/stderr and waits indefinitely; a child can exhaust memory or hang every caller, and the API has no cancellation/deadline/output-budget options. | Add an options-based bounded API with total/per-stream byte limits, timeout/cancellation, explicit truncation status, and a compatibility wrapper. |
| ZRT-190 | Fixed-WT | P0 | After child launch, `std::string::append` or other allocation could throw: POSIX descriptors/child reaping were not RAII-owned, while an exception escaping the Windows capture thread terminated the process. | Route both capture paths through no-throw drain-or-discard sinks, expose independent `capture_failed` state, scope-own POSIX descriptors/actions/PIDs and Win32 handles/processes/threads, terminate/reap on unwind, and exercise deterministic storage failure followed by a clean launch. |
| ZRT-191 | Fixed-WT | P1 | Windows `utf8_to_wide` cast `std::string_view::size()` to `int` twice without rejecting values above `INT_MAX`. | Reject unrepresentable byte counts before either Win32 call, reuse one checked native length, and exercise the exact `INT_MAX`/`INT_MAX + 1` policy boundary through host-neutral test support. |
| ZRT-192 | Fixed-WT | P2 | Windows command-line joining could allocate and quote far beyond CreateProcessW's 32,767-code-unit limit before the OS rejected it. | Apply a lossless UTF-8 impossibility preflight, compute bounded CRT quote expansion, and reject the exact aggregate argument where content, separators, and the required NUL would exceed the native limit. |
| ZRT-193 | Open | P1 | POSIX environment construction walks process-global `environ` while other threads may call environment mutation APIs; the empty-override path passes the live pointer directly to `posix_spawnp`. | Serialize environment snapshot/mutation or use an immutable application-owned baseline and always spawn from a stable copied vector. |
| ZRT-194 | Open | P1 | POSIX pipes are created with `pipe()` and only later marked `FD_CLOEXEC`, leaving a descriptor-inheritance race with concurrent process creation. | Use `pipe2(O_CLOEXEC)` in the Linux adapter and the closest atomic platform primitive elsewhere, retaining the checked fallback only where necessary. |
| ZRT-195 | Open | P2 | Working-directory validation performs `exists`/`is_directory` before launch, but the path can be replaced before the spawn action/CreateProcess consumes it. | Treat the native launch/chdir result as authoritative and, for privileged callers, add a handle-based directory policy rather than relying on preflight checks. |
| ZRT-196 | Fixed-WT | P2 | `StringInterner` move construction cleared the source map but assumed the moved-from `deque` was empty; the standard only guarantees a valid unspecified source state, so storage/map invariants depended on the library implementation. | Explicitly clear source map and storage after a successful transfer, promise an empty reusable moved-from interner, and verify both move construction and assignment restart symbol numbering at one. |
| ZRT-197 | ADR | P1 | `StringInterner::lookupOptional` releases its mutex before returning a borrowed `string_view`; concurrent `truncate`, assignment, or destruction can invalidate the view despite the class being described as thread-safe. | Define pinned/read-guarded or owning lookup semantics in an ADR and reserve borrowed views for externally synchronized users. |
| ZRT-198 | Measure | P2 | `StringInterner::intern` holds one mutex through hash growth, string allocation/copy, deque insertion, and map publication, and calls `reserve(size+1)` for each new symbol. | Benchmark batched reserve plus sharded/read-mostly lookup while preserving stable symbol IDs and rollback guarantees. |
| ZRT-199 | Fixed-WT | P1 | `GrowingArena::destroyObjects` iterated its destructor vector in place; an arena-owned destructor that reentered `create` invalidated the reverse iterator and reproduced as a bus error, while reset/move reentry could invalidate older live targets. | Pop each record before its callback so reentrant creations join the same strict-LIFO drain, guard reset and both move directions during teardown, and cover create-plus-reset reentrancy. |
| ZRT-200 | Open | P1 | `pathFromUtf8` deliberately preserves embedded NUL in `std::filesystem::path`, but most downstream OS filesystem APIs are NUL-terminated and may silently address only the prefix. | Add a central validated external-path constructor/result type and require callers crossing into OS APIs to reject embedded NUL once, rather than auditing ad hoc call sites. |
| ZRT-201 | Fixed-WT | P3 | TextInput capacity checks treated a nonzero numeric capacity as sufficient even if the backing pointer was null, and undo deduplication assumed the live-text pointer was always present; those implicit invariants also obscured analyzer coverage at mutation boundaries. | Require both storage and capacity before reusing the buffer, allocate when storage is absent, compare history against a defensive empty-text view, and keep the set-text copy guarded by the proven pointer. |
| ZRT-203 | Fixed-WT | P0 | `vg_widget_destroy` left the parent live while recursively destroying children, so a child destructor could re-enter and free that parent before the outer frame next read it; focus-loss cleanup could similarly recurse before a child was detached. | Guard active pointer-plus-ID destruction frames, detach children before callback-capable cleanup and recursive teardown, reject hierarchy/focus mutation of a destroying generation, and cover both destructor and focus-loss parent re-entry. |
| ZRT-204 | Fixed-WT | P0 | Tab-order DFS called each widget's public `can_focus` hook before reading that widget and its sibling link again, so a destructive callback invalidated the iterator and left stale pointers in the sort array. | Snapshot only structurally eligible pointer-plus-ID candidates without callbacks, stably sort that snapshot, invoke capability hooks during bounded traversal, and skip destroyed generations while respecting reentrant focus changes. |
| ZRT-205 | Fixed-WT | P0 | `vg_widget_remove_child` and `vg_widget_clear_children` ran focus/tooltip cleanup before unlinking and then mutated the old parent/child, while reparent helpers continued after that cleanup without revalidating either generation. | Complete structural unlink and revision publication before callback-capable cleanup, stop cleanup when its root generation dies, and revalidate both requested parent and child before finishing add/insert operations. |

## Validation record

No full rebuild and no full `ctest` run were performed, per the concurrent-work
constraint. Focused validation completed during this review includes:

- Clang static-analyzer passes for the modified compression, TLS, SSE, HTTP
  client/server, multipart, HTTP connection-pool, generic connection-pool, and
  HTTP/2 translation units.
- Targeted builds and direct runs of `test_rt_network`,
  `test_rt_network_timeout`, `test_rt_network_harden`,
  `test_rt_network_highlevel`, `test_rt_compress`,
  `test_rt_http_client`, `test_rt_http_server`, `test_rt_https_server`,
  `test_rt_multipart`, `test_rt_http2`, `test_rt_errors_io`, and
  `test_rt_binfile`, plus the focused `test_runtime_surface_audit`.
- Focused collection/runtime tests covering pool and GC boundaries, handle
  validation, scheduler/concurrent-map behavior, BinaryBuffer, Bytes,
  BloomFilter, Trie, SaveData, Locale, Audio FX, string/version behavior, and
  the modified collection families.
- Targeted `test_rt_savedata`, `test_rt_ogg`, and `test_ogg_vorbis` builds and
  direct executions for cached SaveData cardinality, Ogg failure outputs,
  multiplexing limits, packet metadata, Vorbis decode, and OGV playback.
- Targeted `test_rt_crypto` build and direct execution for strict policy
  identifiers plus the existing SHA/HMAC/HKDF, AEAD, key agreement, DRBG, and
  approved-mode compatibility matrix.
- Targeted `test_rt_mouse`, `test_vg_audit_fixes`, `test_vg_textinput`,
  `test_vg_tier1_fixes`, `test_vg_tier2_fixes`, `test_vg_tier3_fixes`, and
  `test_vg_widgets_new` builds and direct executions for saturating/non-finite
  input handling, callback-destruction safety, event/focus generation identity,
  hierarchy re-entry, tab order, history, IME, and ordinary widget lifecycles.
- Targeted `test_rt_keyboard` build and direct execution for unique bounded
  frame edges, backend-key translation, text input, and caps-lock hooks; the
  shared input unit also retained a green `test_rt_mouse` run.
- Targeted `test_run_process_quotes` build and direct execution for argv,
  environment, cwd, capture, status, embedded-NUL launch behavior, and the
  Windows UTF-8 narrowing and quoted command-line budget boundaries, plus
  deterministic post-launch capture-storage failure and successful recovery.
- Targeted `test_rt_locale_manager`, `test_rt_memstream`, `test_rt_pool`, and
  `test_support` builds and direct executions for saturated locale references,
  binary floating-point representation assumptions, pool size-class
  disagreements, reentrant arena teardown, and moved-from string interners.
- Targeted `test_rt_frozenmap`, `test_rt_frozenset`,
  `test_rt_handle_validation`, `test_rt_seq_box`, `test_rt_memory_surface`, and
  `test_rt_object_introspect` builds and direct executions for wrong-box,
  wrong-object, forged-pointer, invalid-source-Seq, partial-construction
  rollback, boxed-null, and failed String-retain behavior.
- Targeted `test_rt_seq`, `test_rt_map`, `test_rt_orderedmap`,
  `test_rt_lrucache`, `test_rt_multimap`, `test_rt_treemap`, and
  `test_rt_intmap` builds and direct executions for failure-atomic owning
  mutation, retained removal/peek results, bulk-retain rollback, invalid
  snapshots, map replacement/insertion, LRU order, and MultiMap count
  consistency under returning retain traps.
- Targeted `test_rt_option` plus the returning-hook handle suite for failed
  object, inline-string, and heap-string ownership acquisition in Some/SomeStr,
  while retaining the ordinary Option/Box conversion matrix.
- Targeted `test_rt_queue`, `test_rt_stack`, `test_rt_ring`, `test_rt_deque`,
  `test_rt_collections_correctness`, `test_rt_list`, `test_rt_set`,
  `test_rt_sparsearray`, and the returning-hook handle suite for saturated
  insert/result ownership, refcount-neutral destructive transfers,
  failure-atomic Option pops, clone/snapshot cleanup, unchanged lengths/order,
  and balanced cleanup across owning containers.
- Targeted `test_rt_heap_alloc`, `test_rt_realloc_mem`, and
  `test_rt_memory_surface` builds and direct executions after common
  slab/system allocation provenance headers were introduced.
- Clang static-analyzer passes for the modified GUI event/widget core,
  TextInput, runtime input, crypto, pool, locale, MemStream, arena,
  string-interner, RunProcess, FrozenMap, FrozenSet, Box, and String
  translation units, plus a clean
  `./scripts/lint_platform_policy.sh` run.
- Strict syntax/static-analysis checks on the modified collection, SaveData,
  localization, audio, compression, and networking units.
- Clang static-analyzer passes for the modified Queue, Stack, Ring,
  PriorityQueue, Deque, List, Set, SparseArray, and Option translation units.
- Clang static-analyzer passes for the modified object-array, Iterator, List,
  Ring, SortedSet, Trie, and Seq translation units; targeted
  `test_rt_trie`, `test_rt_handle_validation`, and `test_rt_seq` builds and
  direct runs cover saturated ownership, invalid receivers, transactional
  snapshots, and allocation-free parent-linked traversal across 513-byte keys.
- Clang static-analyzer passes for the shared collection-conversion, Set, Bag,
  Stack, Queue, and Deque translation units; targeted `test_rt_convert_coll`,
  `test_rt_set`, `test_rt_bag`, and `test_rt_handle_validation` runs cover every
  indexed source family, wrong-class operands, saturated retains, variadic
  constructor overflow/rollback, transactional Set algebra, and Bag/Set
  snapshots.
- Clang static-analyzer passes for DefaultMap, BiMap, CountMap, and WeakMap;
  targeted `test_rt_defaultmap`, `test_rt_bimap`, `test_rt_countmap`,
  `test_rt_weakmap`, and `test_rt_handle_validation` runs cover callback
  re-entry during Remove/Clear/finalization, two-index conflict replacement,
  invalid snapshot receivers, exact-capacity transactional snapshots,
  32-bit top-N conversion, and weak probe/compaction rollback.
- Clang static-analyzer passes for Map and IntMap; targeted `test_rt_map`,
  `test_rt_intmap`, `test_rt_handle_validation`, and
  `test_rt_collections_correctness` runs cover detach-first Remove/Clear,
  callback re-entry that clears or reinserts, and owner-finalization re-entry
  for both string-keyed and integer-keyed chain tables.
- Clang static-analyzer passes for TreeMap; targeted `test_rt_treemap`,
  `test_rt_handle_validation`, and `test_rt_collections_correctness` runs
  cover commit-before-release replacement/removal, detached-array Clear,
  callback reinsertion, owner-finalization re-entry, cleanup continuation after
  a trapping value finalizer, and reusable re-armed resurrection.
- Clang static-analyzer passes for OrderedMap, LRUCache, and MultiMap; targeted
  `test_rt_orderedmap`, `test_rt_lrucache`, `test_rt_multimap`,
  `test_rt_handle_validation`, and `test_rt_collections_correctness` runs cover
  whole-table detach, callback-preserving Clear, owner-finalization mutation
  rejection, trapping-finalizer drains, reusable re-armed resurrection, exact
  snapshots, LRU replacement/eviction re-entry, fixed-capacity preservation,
  large logical eviction limits, and returning append hooks.
- A MinGW C++20 warning-clean syntax pass covered the refactored Win32
  `RunProcess` handle/process/thread ownership path from the macOS host.

The final handoff for each implementation tranche must still run the narrowest
relevant test label/executables and `./scripts/lint_platform_policy.sh` when the
change is cross-platform-sensitive. The canonical build and broad test lanes
remain deferred until the concurrently changing tree is ready for them.

## Recommended implementation order

1. Finish P0 returning-trap, finalizer-reentrancy, and forged-handle coverage,
   especially ZRT-010, ZRT-073/ZRT-244/ZRT-263/ZRT-264, ZRT-157, and ZRT-158.
2. Add SaveData byte/entry/string budgets before its indexing/streaming work
   (ZRT-093 and ZRT-100).
3. Continue hostile-network hardening with TLS transcript fuzzing, independent
   WebSocket broadcast progress, and server overload policy (ZRT-138, ZRT-142,
   and ZRT-145).
4. Remove audio-callback locking and sample-rate assumptions (ZRT-149 and
   ZRT-150).
5. Establish microbenchmarks before sharding registries, redesigning scheduler
   and ThreadPool queues, or changing collection/codec storage.
