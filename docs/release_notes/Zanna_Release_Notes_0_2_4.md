---
status: active
audience: public
last-verified: 2026-07-15
---

# Zanna Compiler Platform — Release Notes

> **Development Status:** Pre-Alpha. Zanna is under active development and not ready for production use.

## Version 0.2.4 — Pre-Alpha (2026-04-13)

### What this release is about

This release spans five themes:

- **GPU backend maturity.** Metal and D3D11 reach ~94% parity with the software renderer; OpenGL gains shared infrastructure. 53 per-plan feature implementations across all four backends cover lit textures, spot lights, normal/specular/emissive maps, fog, wireframe, GPU skinning + morph targets, HDR post-FX (bloom / FXAA / tonemap / vignette / color grade), shadow mapping with PCF, instancing, terrain splatting, skybox, and environment reflections.
- **Native toolchain, end-to-end.** `zanna build` goes from Zia source to `.exe` / Mach-O / ELF with zero external tools on all three platforms. From-scratch PE/COFF linker, full ELF dynamic-linking, platform-aware Mach-O symbol resolution, Windows ARM64 import-stub emission, and a new `NativeEHLowering` pass that rewrites structured IL EH into ordinary calls before backend lowering.
- **Worker-VM execution model.** Bytecode VMs spawned for `Thread.Start`, `Async.Run`, and HTTP callbacks own a snapshotted `ExecutionEnvironment` (native handlers + runtime-bridge toggles) and can outlive their parent. Per-VM extern registry, resumable trap state, and per-VM interrupt epoch close the lifetime gaps.
- **3D engine + asset pipeline.** Procedural terrain with LOD + frustum culling, Gerstner water, cross-billboard vegetation, glTF 2.0 / STL / OBJ+MTL / FBX loaders, unified `SceneAsset` asset container, `AnimController3D` state machine, quaternion-based `Physics3D` with CCD + joints, `Collider3D` with 7 reusable shape types, `NavAgent3D` A* pathfinding, 3D audio, video playback (AVI/MJPEG + Theora infrastructure), PBR metallic-roughness materials.
- **2D game engine + asset embedding.** Ten new high-level APIs (Entity, Behavior, Raycast2D, LevelDocument, SceneManager, Camera.SmoothFollow, AnimStateMachine, MenuList.HandleInput, Config.Load, Tilemap.SetTileAnim) and the compile-time ZPAK asset-embedding pipeline for zero-file-dependency native executables.

Cross-cutting groundwork: `PlatformCapabilities.hpp` + CMake capability gating (AUTO / REQUIRE / OFF) + unified Unix build scripts + the new `zanna install-package` toolchain installer.

### By the Numbers

| Metric | v0.2.3 | v0.2.4 | Delta |
|---|---|---|---|
| Commits | — | 119 | +119 |
| Source files | 2,671 | 2,869 | +198 |
| Production SLOC | ~348K | ~450K | +102K |
| Test SLOC | ~165K | ~183K | +18K |
| Test count | 1,351 | 1,454 | +103 |
| Demo SLOC | — | ~177K | — |

Counts via `scripts/count_sloc.sh`.

---

### GPU backends

- **Metal — feature-complete** (14 plans, ~47% → ~94% parity). Direct vs GPU-postfx window paths split; offscreen textures + in-flight command buffers cached per render target; packed morph delta / normal-delta buffers cached by `(morph_key, morph_revision, shape_count, vertex_count)` with age-based pruning. Skybox rewritten as a fullscreen triangle reconstructed from `vertex_id` via inverse-projection + inverse-view-rotation. macOS 26 (Tahoe) compatibility via offscreen `MTLStorageModeManaged` readback.
- **D3D11 — 20 features.** 3,173-line HLSL+C rewrite covering diffuse + normal matrix cbuffers, per-frame SRV cache, HLSL cone-attenuation spot lights, wireframe + cull, fog, normal / specular / emissive maps, RTT with resolve, GPU skinning + morph targets, post-FX suite, shared VBO/IBO pool, systematic `FAILED(hr)` validation, shadow mapping (`COMPARISON_LESS_EQUAL` + PCF), per-instance structured buffer, terrain splatting, TextureCube skybox, environment reflections (cubemap + fresnel), morph normal deltas, DOF / motion blur / SSAO / chromatic aberration. New `windows-d3d11` CI job validates in isolation.
- **Shared backend infrastructure.** Per-backend `_shared.{c,h}` pairs factor out constant-buffer layouts, packed morph weight encoding, blend mode / target kind enums, and frame-history tracking. `vgfx3d_backend_utils.{c,h}` adds RGBA unpack, cubemap face decode, mip-count math, normal-matrix derivation, 4×4 inverse.
- **Cross-backend hardening.** `Pixels` and `CubeMap3D` carry a `uint64_t generation` counter bumped by every in-place mutation; GPU caches compare cached vs current generation before reuse (fixes the "modified texture not updating on screen" class of bugs on all backends). Morph-target generation tracking. Canvas3D post-FX state latched at frame boundaries. Resize callback propagates OS events into `Poll()`. GPU screenshot readback with RTT + `backend->readback_rgba()` + software fallback. `InstanceBatch3D` migrated from `realloc` to `calloc + memcpy + free`; swap-remove copies all three parallel arrays. `Mesh3D.transform` uses inverse-transpose of the upper 3×3 for normals on non-uniformly scaled meshes.
- **Fixes.** Metal lit-texture branch didn't sample diffuse; Metal spot lights fell through to ambient; Metal texture + sampler recreated every draw call; Metal clear-color alpha 0.0 with PostFX inactive; software-backend `vgfx3d_show_gpu_layer` duplicate-symbol crash (vtable dispatch); macOS `drawRect:` CGImage blit covering Metal layer content every frame; Water3D wave normals using single-sine `dydz = dydx` (Gerstner model now corrects per-direction).

### Native toolchain

- **PE/COFF linker (new).** `ArchiveReader` extracts `.lib` members; `SymbolResolver` resolves across objects + archives with on-demand member inclusion; `SectionMerger` merges `.text` / `.rdata` / `.data` / `.bss` with alignment padding; `DeadStripPass` removes sections unreachable from entry-point roots; `ICF` folds sections with matching content + relocations; `RelocApplier` handles `IMAGE_REL_AMD64_*`; `PeExeWriter` produces PE32+ executables with `.idata` import tables and optional headers.
- **ELF dynamic linking (new).** `ElfExeWriter` rewritten for full dynamic-link: `PT_INTERP`, `PT_DYNAMIC`, `.dynsym`, `.dynstr`, `.hash` (SYSV), `.rela.dyn` with `R_X86_64_GLOB_DAT`. Linux x86_64 programs link natively against libc / libm / libpthread / libX11 / libasound without falling back to the system linker. `planLinuxImports()` classifies dynamic symbols into shared libraries via prefix/exact-match tables; `generateDynStubsX8664()` emits 6-byte `jmpq *__got_sym(%rip)` stubs.
- **Linker hardening.** `BranchTrampoline` uses boundary-based AArch64 placement with ±128 MB displacement validation. `SectionMerger::assignSectionVirtualAddresses()` is public (no duplicate VA-assignment). `SymbolResolver::resolveSymbols()` takes a `LinkPlatform` parameter with per-platform prefix tables. `writeCheckedRel32()` validates signed-32 range. Multi-section COFF writer. Mach-O ObjC section flags (`S_CSTRING_LITERALS` / `S_LITERAL_POINTERS` / `S_ATTR_NO_DEAD_STRIP` / `S_COALESCED`) with log2-power-of-two alignment. Mach-O symbol-name fallback (plain + underscore-stripped + underscore-prefixed) and `OBJC_CLASS_$_` / `OBJC_METACLASS_$_` / `OBJC_EHTYPE_$_` normalization. Mach-O symbol-index sentinel fix (slot 0 no longer means "not found"). Windows ARM64 import stubs (`ADRP` / `LDR` / `BR` with `kPageRel21` / `kPageOff12L`). Table-driven import plans. `DynamicSymbolPolicy.hpp` extracted from `SymbolResolver`. Proper zerofill (`S_ZEROFILL` / `SHT_NOBITS`). Dead-strip applies to every object. String-dedup compaction. ICF cross-object resolution through the global symbol table. `-lshell32` added on Windows for `DragQueryFile`/`DragAcceptFiles`.
- **NativeEHLowering (new pass, 683 LOC).** Rewrites structured IL EH markers (`EhPush` / `EhPop` / `ResumeSame` / `ResumeNext` / `TrapErr`) into ordinary calls and branches before backend lowering. Both x86_64 and AArch64 `CodegenPipeline` invoke it before MIR lowering. 171-line unit test validates all transformations.
- **Fixes.** ELF writer emitted `STT_FUNC` for rodata; COFF `validateCoffRelocationAddend()` diagnostic for unsupported addends and `.pdata` function length field emitted (was 0); missing `libzanna_rt_game.a` in archive discovery.

### VM & runtime

- **Worker VM execution environment.** `ExecutionEnvironment` (runtime-bridge toggle + threaded-dispatch toggle + native-handler map) captured / applied / copied across VMs. `Thread.Start`, `Async.Run`, and the HTTP-server request dispatcher all call `copyExecutionEnvironmentFrom(parent)` on workers so each owns its own native-handler copy.
- **Per-VM extern registry.** Resolution order: per-VM → process-global → built-in `RuntimeDescriptor`. Per-registry mutex for both registration and lookup. `PerVMExternRegistryTests` covers per-VM-only registrations, override of process-global names, and sibling-VM isolation.
- **Resumable trap state.** `BytecodeVM::TrapRecord` snapshots kind, error code, fault PC, next PC, fault line, value / call / EH stacks, and live `alloca` storage — a trap escaping `try/catch` in one frame re-raises cleanly in the caller.
- **Per-VM interrupt epoch.** `std::atomic<uint64_t>` epoch replaces shared `atomic<bool>`; each VM tracks its own observed cursor. Multiple coexisting VMs (REPL + background thread, embedding host + child eval) observe Ctrl-C independently. `clearInterrupt` advances a cleared-epoch watermark.
- **Object lifetime.** Zeroing weak references (`rt_weak_load`/`rt_weak_store` through `rt_weakref_*` handles with `rt_weakref_is_handle` discriminator for raw pointers). `Object.ToString` returns a fresh `rt_string` for string targets and validates the heap header before dereferencing the vptr. `MessageBus.Publish` uses snapshot semantics so subscribers can unsubscribe themselves mid-publish.
- **CALL_NATIVE widened.** `[opcode:8][nativeIdx:8][argCount:8]` (max 255) → `[opcode:8][argCount:8][nativeIdx:16]` (max 65,535). Bytecode format version bumped 1 → 2.
- **Trap + error plumbing.** `rt_trap_set_ip` / `rt_trap_get_ip` store the native IP at trap time (`TrapGetIp` in `runtime.def`). `rt_err_to_trap_kind` maps `RtError` codes (overflow=1, invalid cast=2, domain=3, bounds=4, file-not-found=5, …). `rt_trap_error_make` / `rt_trap_raise_error` combine message storage + field classification + trap invocation.
- **Input subsystem.** `Keyboard.CapsLock` queries real OS state every call (`GetKeyState(VK_CAPITAL)` / `CGEventSourceFlagsState` / `XkbGetIndicatorState`) instead of toggle-counting. `Mouse.SetPos(x, y)` dispatches to `CGWarpMouseCursorPosition` / `SetCursorPos` / `XWarpPointer`. Double-precision wheel deltas (`Mouse.WheelXF` / `WheelYF`). Canvas destruction detaches input bindings via `rt_keyboard_clear_canvas_if_matches` / `rt_mouse_clear_canvas_if_matches`.
- **Fixes.** `rt_exc_is_exception()` accepted any non-null pointer; OOP destructor chaining missing (derived dtors never called base); OOP refcount imbalance (`NEW` objects ended at refcount 2); TLS RSA-PSS SHA-384/512 hashed with SHA-256; bytecode VM `LOAD_I32` / `LOAD_STR_MEM` / `STORE_STR_MEM` routed to unimplemented-trap; POSIX `ProcessIsolation::runIsolated()` blocked forever (no timeout, now WNOHANG poll + `SIGKILL`); `Pixels.Blur` / `Pixels.Resize` unpacked ARGB instead of RGBA (R and A swapped on non-opaque alpha); Windows `Zanna.Threads` test disabled but runtime implemented.

### Compiler & codegen

- **Zia language features (7 new).** Type aliases (`type Name = TargetType;`); variadic parameters (`func sum(nums: ...Integer)`, collected as `List[Integer]`); shift operators (`<<` / `>>`, arithmetic right shift, correct precedence between additive and comparison); compound bitwise assignments (`<<=` / `>>=` / `&=` / `|=` / `^=` desugared via read-op-store); single-expression functions (`func f(x: T) -> R = expr;`); lambda expressions (`(x: T) => expr` and `() => expr`); polymorphic `is` matching any subclass via `collectDescendants()`. Keyword rename: `entity` → `class`, `value` → `struct` across all source, tests, REPL, LSP, runtime GUI, docs, VS Code extension, and website.
- **Zia frontend.** `rtgen` emits full ABI-shaped extern signatures (so string-returning runtime methods compare via `Zanna.String.Equals` instead of pointer-`ICmpEq`); typed return metadata (`obj<ClassName>`, `seq<str>`) for 100+ factory/conversion/collection methods; new `concreteRuntimeReturnClassQName` API + BASIC `inferObjectClassQName` recursive type tracer; runtime property setters (`ctrl.VelocityY = value` calls the setter); `final` enforcement in sema (V3000 error on reassignment of `final` vars, for-in loop vars, match pattern bindings) + lowerer safety net; `catch(e)` binding via `rt_throw_msg_set`/`_get` TLS + `ErrGetMsg` IL opcode; `String.Contains()` alias to `StrHas`; `main()` entry-point parity with `start()` (interface itable init + global initializers); optional struct boxing via `emitBoxValue` (no more dangling stack pointers); completion runtime APIs (`Zanna.Zia.Completion.Check` / `.Hover` / `.Symbols`) via error-tolerant partial compilation.
- **BASIC frontend.** `FoldBuiltins.cpp` evaluates 25+ builtins (`ABS`, `INT`, `SGN`, `SQR`, `LOG`, `EXP`, `SIN`, `COS`, `TAN`, `ATN`, `ASC`, `CHR$`, `LEN`, `LEFT$`, `RIGHT$`, `MID$`, `STR$`, `VAL`, `STRING$`, `SPACE$`, `LCASE$`, `UCASE$`, `LTRIM$` / `RTRIM$` / `TRIM$`) at compile time when arguments are constant.
- **x86_64 backend.** CoffWriter cross-section symbols (rodata first; text relocations redirect to defined entries, fixes LNK2001). `mapRuntimeSymbol()` translates IL runtime names to C names. Operand materialisation for `TESTrr` and `call.indirect` immediates (no more `bad_variant_access`). SETcc REX prefix correct for SPL/BPL/SIL/DIL. SSE RIP-relative `MOVSD` encoding. Spill-slot reuse disabled where cross-block liveness wasn't accounted for. Branch relaxation (short JMP + short Jcc, 2 bytes each). Pipeline decomposition (`legalizeModuleToMIR` / `allocateModuleMIR` / `optimizeModuleMIR` / `emitMIRToAssembly` / `emitMIRToBinary` as public APIs). `CallArgLayout` + `FrameLayoutUtils` extracted as shared utilities. System linker shell-out removed (~330 lines of `cc` / `ld` invocation); native linker is default, `--system-link` deprecated.
- **AArch64 backend.** Register-allocator protected-use eviction (`protectedUseGPR_` / `protectedUseFPR_`) prevents clobbering current-instruction sources — fixes `BowlingGame.init` GEP-base crash under register pressure. `isUseDefImmLike` operand roles classify operand 0 as DEF-only (AddRI, SubRI, …). FPR load/store classification in `isMemLd` / `isMemSt`. Clean FPR spill-slot reuse for rodata values surviving calls in caller-saved FPRs. Live-out spill fix (`isLiveOut()` query prevents premature release; forward end-of-block scan). `TrapErr` materialises the message string into x0 — native `throw "msg"` no longer produces empty diagnostics. Error-field extraction via TLS (`rt_trap_get_kind/code/line`) for typed `catch(e: DivideByZero)` in native code. Apple M-series scheduler tuning (FP divide 3→10, int divide 3→7, FP multiply 3→4 — matches Firestorm latencies). Join-phi coalescing peephole (Pass 4.88) eliminates stack round-trips at CFG join blocks. CBR edge-block generalization (edge blocks emit whenever branch arguments are present, not only when both arms target the same block). Loop phi-spill multi-block latch fix. Pipeline decomposition via `PassManager` (Scheduler + BlockLayout passes at O1+). Virtual register space partitioned (`kFirstVirtualRegId=1`, `kPhiVRegStart=40000`, `kCrossBlockSpillKeyStart=50000`). Branch relaxation (`computeFunctionLabelOffsets` + `measureInstructionSize` + `estimateFunctionSize`; new `A64CondBr19` relocation for conditional branches exceeding ±1 MB). `FrameBuilder` spill-epoch guards prevent stale-offset reuse.
- **IL optimizer.** EarlyCSE / GVN textual-ordering fix: `AvailableExpr{value, block}` + `isTextuallyAvailable()` prevents replacements that would create textual use-before-def. Mem2Reg single-predecessor + entry-block fix (forward from predecessor or use typed zero constant; no unbindable synthetic block parameters). SCCP preserves dominators + loop info + CFG instead of conservatively invalidating.

### 3D engine

- **Terrain system.** `Terrain3D.GeneratePerlin(noise, scale, octaves, persistence)` writes the heightmap from a PerlinNoise object. Three LOD levels per chunk (step 1/2/4 → 578/162/50 verts) by camera distance. Per-chunk AABB + Gribb-Hartmann frustum planes. Downward skirts hide T-junction cracks at LOD seams. 16-bit heightmap (R+G = 65,536 levels). 4-layer splat blending with per-layer UV-tiled textures.
- **Water system.** Gerstner waves (`Water3D.AddWave(dirX, dirZ, speed, amplitude, wavelength)`, up to 8) with per-direction derivative-based normals. `SetTexture` / `SetNormalMap` / `SetEnvMap` / `SetReflectivity`. `SetResolution(n)` (8–256, default 64).
- **Vegetation (new).** Cross-billboard blades (two perpendicular quads per blade — no angle-pop). `Populate(terrain, count)` scatters blades, optionally density-weighted. Per-blade Y-shear wind (`sin(position + time)`) with `SetWindParams(speed, strength, turbulence)`. Distance LOD with progressive thinning + hard far-cull. GPU instancing via `submit_draw_instanced` hook for single-draw-call rendering.
- **Asset loaders.** glTF 2.0 (`.gltf` + `.glb`): mesh + tangents, PBR metallic-roughness, skeletal animation with joint hierarchy + inverse bind matrices, morph targets. STL (binary + ASCII auto-detect). OBJ `.mtl` (Kd/Ks/Ns/d + texture paths, ≤64 materials). FBX (Texture-node path extraction via connection tracing; BlendShape/Shape morph targets). `SceneGraph.Save` → JSON node hierarchy.
- **SceneAsset + AnimController3D.** `SceneAsset.Load(path)` routes by extension (.vscn, .fbx, .gltf, .glb), builds an internal resource collection; `Instantiate()` clones the node tree with shared resources; `InstantiateScene()` returns a fresh `SceneGraph`. `AnimController3D` (871 LOC) adds named states, crossfade transitions, play/pause/stop, speed control, loop modes (once/loop/pingpong), event frame callbacks. `SceneNode.BindAnimator` + `SceneGraph.SyncBindings(dt)` drive automatic skeletal pose updates.
- **Physics3D.** Quaternion orientation (`Body3D.Orientation` + `AngularVelocity` + `ApplyTorque` + `ApplyAngularImpulse` + damping). Dynamic / static / kinematic body modes. Sleep system (`CanSleep`, `Sleeping`, `Wake()`, `Sleep()`, `PH3D_SLEEP_DELAY`). CCD substeps (`UseCcd`, `PH3D_MAX_CCD_SUBSTEPS = 16`). Mass-only constructor. World queries (`Raycast`, `RaycastAll`, `SweepSphere`, `SweepCapsule`, `OverlapSphere`, `OverlapAABB` returning `PhysicsHit3D` / `PhysicsHitList3D`). Collision events with contact-manifold array. `DistanceJoint3D` + `SpringJoint3D` with 6-iteration sequential impulse solver.
- **Collider3D (new, 822 LOC).** 7 reusable shape types: box, sphere, capsule, convex hull (from Mesh3D), triangle mesh, heightfield (from Terrain3D), compound (parent + child transforms). Shapes decouple geometry from body instances; `Body3D.Collider` attaches a shape and AABB / narrow-phase dispatch use the attached collider.
- **NavAgent3D (new, 548 LOC).** Autonomous pathfinding on NavMesh3D: A* path query, string-pulling corridor smoothing, configurable speed / acceleration / stopping distance. `SetDestination(x,y,z)` triggers path computation; `Update(dt)` advances along the corridor; `BindNode(node)` syncs a `SceneNode`.
- **3D audio.** `AudioListener3D` (position + forward + velocity, `BindNode` / `BindCamera`). `AudioSource3D` (positional emitter, inner/outer cone, min/max distance, inverse-distance rolloff, looping, pitch, gain). `Audio3D.SyncBindings(dt)` batch-updates all bound positions from scene nodes.
- **PBR materials + shader hooks.** `Material3D.NewPBR(metallic, roughness, ao)` with metallic-roughness workflow; albedo / MR / AO / normal / emissive slots. `Clone` / `MakeInstance` for shared-base + per-instance overrides. `AlphaMode` (opaque/mask/blend), `DoubleSided`, `NormalScale`. Cook-Torrance BRDF with GGX + Schlick fresnel across all 4 backends. `SetShadingModel(model)` (0=BlinnPhong, 1=Toon, 4=Fresnel, 5=Emissive); `SetCustomParam(index, value)` passes 8 floats per material to the shader.
- **Other.** `Light3D.NewSpot` (position, direction, inner/outer cone, smoothstep). `Camera3D.NewOrtho` for isometric/strategy. `Mesh3D.Clear()` resets counts without freeing. Sprite3D use-after-free fix (cached instances + GC temp buffer). Cubemap bilinear filtering (4-texel interpolation). `ZANNA_3D_BACKEND=software` env override forces the software renderer.

### 2D game engine

- **Ten new high-level APIs.** `Entity` (New / ApplyGravity / MoveAndCollide / UpdatePhysics / Overlaps / AtEdge / PatrolReverse, 282 LOC); `Behavior` (AddPatrol / AddChase / AddGravity / AddEdgeReverse / AddShoot / AddSineFloat / Update, 227 LOC); `Raycast2D` (HasLineOfSight + Collision.LineRect / LineCircle, 124 LOC); `LevelDocument` (JSON load + ObjectCount/Type/X/Y + PlayerStartX/Y + Theme, 222 LOC); `SceneManager` (Add / Switch / SwitchTransition / Update / Current / IsScene, 183 LOC); `Camera.SmoothFollow(targetX, targetY, speed)` + `SetDeadzone(w, h)` (45 LOC); `AnimStateMachine` (AddNamed / Play(name) / StateName / SetEventFrame / EventFired, 76 LOC); `MenuList.HandleInput(up, down, confirm)`; `Config.Load(path)` + GetString/Int/Bool/Float/Has/Keys (114 LOC); `Tilemap.SetTileAnim` + SetTileAnimFrame / UpdateAnims / ResolveAnimTile (76 LOC).
- **Other game runtime.** `Timer.StartMs` / `UpdateMs` / `ElapsedMs` / `RemainingMs` for delta-time-independent cooldowns. `Lighting2D` darkness overlay with pulsing player light + pooled dynamic point lights. `PlatformerController` (jump buffering, coyote time, variable jump height, ground/air accel, apex gravity bonus). `AchievementTracker` (64-achievement bitmask + 32 stat counters + animated slide-in popups). `Typewriter` character-by-character reveal. `GameButton` styled widget (132 LOC).
- Game engine classes moved `src/runtime/collections/` → `src/runtime/game/` (36 files; no API changes).
- **Fixes.** `PlatformerController` velocity desync after damage knockback, death bounce, enemy stomp. `Particle` emitter zero-alpha `Color.RGB()` values rendering as opaque (alpha byte 0 = opaque).

### Asset embedding (ZPAK)

Compile-time asset packaging for zero-file-dependency native executables. Assets compile into a binary blob at build time and embed into the executable's `.rodata`.

- **Project directives** in `zanna.project`: `embed <path>` (raw bytes), `pack <path>` (ZPAK container framing), `pack-compressed <path>` (DEFLATE).
- **Toolchain.** `AssetCompiler` (233 LOC) reads directives, resolves paths, invokes `ZpakWriter` (230 LOC, magic header + file table + concatenated data + optional DEFLATE). `ZpakReader` (`rt_zpak_reader.c`, 375 LOC) mounts ZPAK archives at runtime with multi-archive support. Both x86_64 and AArch64 `CodegenPipeline` inject the ZPAK blob as `zanna_asset_blob` / `zanna_asset_blob_size` globals in `.rodata`. `Path.ExeDir()` resolves the executable directory cross-platform (`_NSGetExecutablePath` / `/proc/self/exe` / `GetModuleFileName`).
- **Runtime API (`Zanna.IO.Assets`).** `Load(name)` / `LoadBytes(name)` / `Exists(name)` / `Size(name)` / `List()` / `Mount(path)` / `Unmount(path)`. Resolution order: embedded blob → mounted ZPAK archives → filesystem fallback relative to the executable directory. 9 ZPAK format tests + 9 asset manager tests.
- **Fixes.** ZAPS DEFLATE double-free crash. `.lnk` shortcuts missing LinkInfo structure.

### GUI

- **Per-app theme ownership.** Each `rt_gui_app_t` owns a private scaled theme copy; `vg_theme_dark()` / `vg_theme_light()` singletons are no longer mutated.
- **Widget runtime state.** Focus + keyboard capture + tooltip state save per-app and restore on activation. Modal dialog stack routes via the actual `dialog_stack` array with `rt_gui_sync_modal_root`, replacing the parallel event path.
- **Platform text input.** `VGFX_EVENT_TEXT_INPUT` carries translated Unicode from the OS input method (macOS `interpretKeyEvents:` / `insertText:`, Win32 `WM_CHAR`, X11 `XLookupString`). `KEY_CHAR` delivery replaces US-layout ASCII synthesis (fixes non-QWERTY keyboards + dead keys).
- **Widget tree + hover + overlay pass.** `hovered_widget` tracking with `VG_EVENT_MOUSE_ENTER` / `LEAVE` on transitions across capture / modal / normal hit-test paths. Modal keyboard fallback routes unhandled keys to the modal root. `vg_widget_set_focus(NULL)` releases the previously focused widget. Two-pass painting (normal + overlay); widgets that paint children internally (`ScrollView`, custom `paint_overlay`) act as render boundaries. `FloatingPanel` reparents children into the real widget tree (previously a private array). `Dropdown` open popup moved into `paint_overlay` with screen-bounds hit testing. `ScrollView` measures children before content-size computation, clips rendering via `vgfx_set_clip`, and walks the parent chain in `scroll_to_widget`.
- **Layout.** `VBox` / `HBox` / `Flex` distribute leftover main-axis space by `justify` mode (`START` / `CENTER` / `END` / `SPACE_BETWEEN` / `SPACE_AROUND` / `SPACE_EVENLY`) via `compute_justify_distribution()`. `vg_container_set_spacing` polymorphic setter.
- **Native macOS menu bar.** 554-line `rt_gui_macos_menu.m` mirrors Zanna GUI menubars to the native app menu. About / Preferences / Quit relocation to the app menu (Cmd+, / Cmd+Q). Standard items auto-generated (Services, Hide / Hide Others / Show All). Zanna `Ctrl+` → macOS `Cmd+` accelerator translation with full function / arrow / modifier mapping. Menu bar suppression when `native_main_menu` is active. Window / Help menu registration with `NSApp`.
- **Other.** Dropdown placeholder strings copied (no use-after-free on freed temporaries). Dismissed notifications compacted immediately. UTF-8 command palette query path. MessageBox default/cancel button semantics. Font inheritance applied at construction. `vg_codeeditor.c` substantially expanded. `SplitPane.New(parent, horizontal)` second arg now treated as `horizontal != 0`.
- **Fixes.** Overlay timing moved from last-input-event timestamps to wall-clock via `rt_gui_now_ms`. macOS Cmd+key shortcuts now consumed by the native menu bar (arrow keys stop triggering system beeps). GUI menubar accelerator table no longer leaks on destroy.

### Graphics foundation (vgfx)

- **Two-phase event/present.** `vgfx_pump_events(window)` drains the native queue without flipping the framebuffer; `rt_canvas_poll()` calls it first so input is available before rendering. Event payload structs documented across `vgfx.h` (`VGFX_EVENT_TEXT_INPUT.codepoint`, `VGFX_EVENT_SCROLL.delta_x/y/x/y`, focus-gained / focus-lost). Shared `vgfx_internal_resize_framebuffer()` factored out of macOS for Linux / Win32 / mock backends.
- **Linux X11.** `Button4/5/6/7` → `VGFX_EVENT_SCROLL`; `XLookupString` → UTF-8 `VGFX_EVENT_TEXT_INPUT`; `FocusIn` / `FocusOut` → focus events; `ConfigureNotify` → shared resize; key-state map alongside the event stream.
- **Windows Win32.** `WM_MOUSEWHEEL` / `WM_MOUSEHWHEEL` → scroll with high-resolution deltas; `WM_CHAR` → text-input with surrogate-pair handling (UTF-16 → codepoint); `WM_SETFOCUS` / `WM_KILLFOCUS` → focus; `WM_SIZE` → shared resize; raw-input keyboard state for `vgfx_key_down()`.
- **macOS.** Removed duplicate inline resize body (shared helper now owns it). Explicit `is_focused` updates in `windowDidBecomeKey:` / `windowDidResignKey:`.
- **HiDPI.** Dynamic backing scale queried per frame (windows dragged across mixed-scale displays report correctly immediately). `vgfx_get_monitor_size` uses the window's actual current monitor when the backend can determine it; primary-monitor fallback only when no window is bound. Resize events carry both physical-pixel `width`/`height` and logical `logical_width`/`logical_height`.
- **Mock backend helpers.** `vgfx_mock_inject_text_input(win, codepoint)`, `vgfx_mock_inject_scroll(win, dx, dy, x, y)`, `vgfx_mock_inject_focus(win, focused)`.

### Media codecs

- **Image formats.** JPEG load (baseline DCT, 8-bit, YCbCr / grayscale, 4:4:4 / 4:2:0 / 4:2:2, EXIF orientation, restart markers); PNG load + save (all 5 color types, 1/2/4/8/16-bit, PLTE + tRNS, Adam7 interlace, round-to-nearest 16→8); GIF load (GIF87a/89a, LZW, ≤64-frame animation, all 4 disposal methods, interlacing, per-frame delay); BMP load + save (24-bit uncompressed). `Pixels.Load(path)` auto-detects by magic bytes; `Sprite.FromFile(path)` covers all four with animated-GIF frame timing.
- **Audio formats.** WAV (8/16/24/32-bit PCM + 32-bit IEEE float, any sample rate → 44100 Hz resample); OGG Vorbis (codebook VQ, floor type 1, multi-pass residue types 0/1/2, FFT-based IMDCT, stereo coupling, streaming); MP3 (MPEG-1/2/2.5 Layer III, ID3v2 + ID3v1, bit reservoir, streaming); VAF (IMA ADPCM 4:1 block-based). Music streaming uses the same triple-buffer architecture (~96 KB regardless of track length) with seamless looping.
- **Audio streaming.** Multi-stream OGG via `ogg_reader_next_packet_ex()` returning per-packet `ogg_packet_info_t` (serial number, granule position, BOS/EOS flags); Vorbis stream selection identifies the first Vorbis BOS packet, enabling correct `.ogv` (Theora + Vorbis) extraction. Unified `vaud_music_seek_output_frame()` handles seek / rewind / loop-restart for all formats. `source_sample_rate` separated from mixer rate. OGG duration from last-page granule scan. Loop restart consolidation removed 40+ lines of format-specific logic.
- **Video playback.** `VideoPlayer` runtime class (Open / Play / Pause / Stop / Seek / Update / SetVolume / Width / Height / Duration / Position / IsPlaying / Frame). AVI/MJPEG decoder (RIFF chunk tree walker, MJPEG DHT injection — 420-byte Annex K tables before SOS — `rt_jpeg_decode_buffer` extracted from file-based loader). Theora infrastructure (identification 0x80 / comment 0x81 / setup 0x82 header parsing, YCbCr 4:2:0 → RGBA via BT.601 in `rt_ycbcr.c`, OGG multi-stream demux; reference Y/Cb/Cr plane buffers allocated — full DCT/MC is a follow-up). `VideoWidget.New(parent, path)` creates an image widget driven by `Update(dt)`.
- **Fixes.** PNG 16-bit sample downscaling discarded LSB (now round-to-nearest). MP3 ID3v1 tags at end of file could corrupt last decoded frame. MJPEG AVI decode failed without DHT (now injects standard Annex K tables). GUI `Image` widget had no `paint` vtable (content never rendered).

### Networking

- **HttpServer runtime class (new).** `Listen` / `Accept` / `Respond` / `Close` / `Method` / `Path` / `Header` / `Body` wired through bytecode VM + both Zia and BASIC frontends.
- HTTP callbacks dispatch to worker VMs inheriting parent runtime-bridge toggles + native handler registrations safely via the per-VM extern + worker-VM execution environment above.
- **Fixes.** Network test on macOS (`getaddrinfo(NULL, …)` with `AF_UNSPEC` prefers IPv6 — test accepts `"::"` or `"0.0.0.0"`). `test_rt_network_highlevel` SSE chunked mock server (wrong hex chunk size `0x14` for 23-byte payload — now `0x17` — and missing trailing `\r\n` per RFC 7230 §4.1). Cipher `rt_cipher_decrypt` didn't fall back to legacy HKDF when PBKDF2-derived key failed.

### I/O runtime

- **SaveData.** `SaveEntry` migrated from raw `char*` to `rt_string` keys/values. JSON parse error forwarding via `rt_json_stream_error()`. Versioned format with future migration support.
- **Glob pattern matching.** Character classes (`[a-z]`, `[0-9]`, `[!abc]`), case-insensitive matching on Windows (via `tolower` normalization), `**` recursive descent, correct path separator handling (`*` doesn't cross `/` or `\`).
- **File watcher.** Debounced event coalescing, single-file watch via parent-directory + leaf-name filter, Windows `OVERLAPPED.hEvent` leak fix, non-empty event paths for single-file watches.
- **TempFile.** Atomic `O_CREAT|O_EXCL` (POSIX) / `CREATE_NEW` (Windows) with collision retry — eliminates TOCTOU race.
- **Archive extraction.** Path-traversal validation rejects `../` directory-escape entries (zip-slip).

### Cross-platform & packaging

- **`PlatformCapabilities.hpp` + CMake capability gate.** Shared C++ header in `src/common/` with `ZANNA_HOST_*` / `ZANNA_COMPILER_*` / `ZANNA_CAN_*` macros. `ZANNA_GRAPHICS_MODE` and `ZANNA_AUDIO_MODE` CMake cache vars with AUTO / REQUIRE / OFF modes replace silent `return()` in library `CMakeLists.txt` that could produce broken binaries.
- **Generated `RuntimeComponentManifest.hpp`.** Machine-checked archive-name → component mapping generated from `runtime.def`, replacing hand-maintained string tables — drift between `runtime.def` and linker discovery is now a build error.
- **Platform import planners.** `NativeLinker.cpp` monolith (~1100 lines) split into `PlatformImportPlanner.hpp` + `MacImportPlanner.cpp` + `LinuxImportPlanner.cpp` + `WindowsImportPlanner.cpp`. Each planner owns its platform's symbol → dylib/DLL classification.
- **Unified build scripts.** `build_zanna_unix.sh` replaces near-identical `build_zanna_mac.sh` / `build_zanna_linux.sh` with `uname -s` detection; old scripts kept as thin wrappers. Standard env vars (`ZANNA_BUILD_DIR`, `ZANNA_BUILD_TYPE`, `ZANNA_SKIP_INSTALL`). `scripts/lint_platform_policy.sh` flags raw `_WIN32` / `__APPLE__` / `__linux__` outside approved adapter files. `scripts/run_cross_platform_smoke.sh` runs the appropriate ctest label slice + example smoke probes. `PlatformSkip.h` + CTest `SKIP_RETURN_CODE 77` makes skips visible in CI output.
- **Toolchain packaging.** `ToolchainInstallManifest` (`src/tools/common/packaging/`) data model packages Zanna itself from a staged `cmake --install` tree (distinct from `PackageConfig` which packages apps built WITH Zanna). New `zanna install-package` CLI with `--target` / `--arch` / `--stage-dir` / `--build-dir` / `--verify-only` / `--no-verify` / `--keep-stage-dir` / `--stage-only` / `--metadata-file`. Platform builders: `buildWindowsToolchainInstaller`, `buildMacOSToolchainPkg`, `buildLinuxToolchainPackages`. `scripts/build_installer.sh` and the platform wrappers stage via `cmake --install` then invoke the CLI. `InstallPackageTarballSmoke.cmake` + `InstalledZannaConfigSmoke.cmake` CTest integration tests. Installer plans at `misc/plans/installer/` with Phase 7 signing/release scaffolding.
- **Installed runtime library discovery.** `LinkerSupport.cpp` layered search: `ZANNA_LIB_PATH` env → exe-relative `../lib/` → platform standard paths (`/usr/lib/zanna`, `/usr/local/zanna/lib`, …) → build-tree fallback. Companion resolvers for `zannagfx` / `zannaaud` / `zannagui` threaded through both codegen pipelines so an installed Zanna compiles native executables without a build tree. `test_linker_support.cpp` covers all four discovery tiers. `test_runtime_surface_audit.cpp` validates that every runtime-component archive the discovery code looks for is actually produced by the build. Exported target set in `ZannaTargets.cmake` reconciled with the actual built runtime libraries.
- **Linux native build.** ELF writer extended with GOT/PLT-style dynamic relocation support. ELF / COFF / Mach-O writers hardened for alignment + section flags + symbol ordering. OpenGL 3D backend Linux init fixes, PBR uniform forwarding, shader-compilation guards. Software 3D backend platform-gated overrides (non-graphics builds no longer pull GPU symbols). x86_64 LowerDiv IDIV RDX clobber fix for Linux ABI. Runtime ALSA / crypto-TLS / threading / network test portability fixes.

### Tests

Roughly 18K lines of new coverage (`+103` tests to 1,454). Highlights:

- **GPU paths.** `test_rt_canvas3d_gpu_paths` (595 LOC), `test_vgfx3d_backend_utils` (224 LOC), `test_vgfx3d_backend_{metal,d3d11,opengl}_shared.{c,m}`.
- **Game engine.** `test_rt_entity` / `_behavior` / `_raycast_2d` / `_scene_manager` / `_animstate_named` / `_camera_enhance` / `_game_menu` / `_tilemap_anim` / `_zpak_format` / `_asset_manager` / `_path_exe_dir`.
- **Native linker.** `test_native_linker`, `test_branch_trampoline` (boundary placement), `test_reloc_applier` (COFF AArch64 BRANCH26), `test_symbol_resolver` (platform-aware), `test_coff_writer` (multi-section), `test_elf_writer` (`STT_OBJECT`), `test_pe_writer`.
- **Codegen.** 4 AArch64 CBR edge-block tests, x86_64 call ABI tests, AArch64 frame spill reuse regression, AArch64 block-layout + scheduler unit tests, Mem2Reg non-entry-block test.
- **VM.** `PerVMExternRegistryTests`, `HttpServerRuntimeTests` + `ThreadsRuntimeTests` for environment-copy semantics, bytecode trap-resume coverage.
- **Runtime.** `RTArgs`, `RTArchive` (path traversal + round-trip), `RTGlob`, `RTSaveData`, `RTWatcher`, `RTFileExt`, `RTGC`, `RTLineWriter`, `RTMouse` (`clear_canvas_if_matches`), `RTMsgBus`, `RTObjectIntrospect`.
- **Smoke probes.** 6 Zia probes (`zia_smoke_paint`, `_zannaide`, `_zannasql`, `_3dbowling`, `_chess`, `_xenoscape`) exercise real example app/game module stacks deterministically.
- **vgfx input.** T22–T25 (pump-events-without-present, text-input event, scroll event, focus state sync).
- **Windows infrastructure.** ProcessIsolation `registerChildFunction()` with indexed dispatch (`--zanna-child-run=N`); failures reduced 48 → 4. Codegen test assertions accept `.rdata` alongside `.rodata` and `cmovne` alongside `cmovneq`. `rtgen` wraps generated `ZiaRuntimeExterns.inc` calls in per-namespace lambdas to dodge MSVC Debug stack overflow. `WinDialogSuppress.c` linked into zanna/zbasic/zia to suppress CI crash dialogs. Windows `CrossLayerArithTests` `dispatchChild()` guard prevents infinite process recursion.

### Demos & docs

Marquee demos: XENOSCAPE (17K-LOC Metroid-style sidescroller), 3D bowling (3.1K LOC), 3D world starter, baseball season simulator (plans 10–21), ZannaSQL (9 bug fixes covering modulo, REPLACE parsing, JOIN IS NULL, window PARTITION BY, LIKE case sensitivity, ALTER TABLE locking, CSV validation, persistence NULL crash, distinct hash function), ZannaIDE, Chess. Docs: comprehensive 186-file audit, full Doxygen sweep across the runtime (~12,400 `/// @brief` tags across `src/runtime/`), Bible appendix migrated from `entity`/`value` to `class`/`struct` terminology, Bible chapters refreshed to the `error[V3000]:` format, networking examples rewritten against actual `Http.Get` / `HttpReq` / `HttpRes` surface.

---

### Commits

See `git log <v0.2.3-anchor>..HEAD -- .` for the full 119-commit history. Commits pair feature-add and follow-up hardening in the same subsystem (e.g. Metal feature plan → cache generation tracking; PE/COFF linker → ICF cross-object resolution; AArch64 regalloc feature → protected-use eviction; Physics3D → CCD sleep-system hardening).
