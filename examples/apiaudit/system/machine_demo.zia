// =============================================================================
// API Audit: Zanna.System.Machine - System Information
// =============================================================================
// Tests: get_Cores, get_Endian, get_Home, get_Host, get_MemFree, get_MemTotal,
//        get_Os, get_OsVer, get_Temp, get_User
// =============================================================================

module MachineDemo;
bind Zanna.Terminal;
bind Zanna.System.Machine as Machine;
func start() {
    Say("=== API Audit: Zanna.System.Machine ===");

    // --- get_Os ---
    Say("--- get_Os ---");
    Say("OS:");
    Say(Machine.get_Os());

    // --- get_OsVer ---
    Say("--- get_OsVer ---");
    Say("OSVer:");
    Say(Machine.get_OsVersion());

    // --- get_Cores ---
    Say("--- get_Cores ---");
    Say("Cores:");
    SayInt(Machine.get_Cores());

    // --- get_MemTotal ---
    Say("--- get_MemTotal ---");
    Say("MemTotal (bytes):");
    SayInt(Machine.get_MemoryTotal());

    // --- get_MemFree ---
    Say("--- get_MemFree ---");
    Say("MemFree (bytes):");
    SayInt(Machine.get_MemoryFree());

    // --- get_Endian ---
    Say("--- get_Endian ---");
    Say("Endian:");
    Say(Machine.get_Endian());

    // --- get_Host ---
    Say("--- get_Host ---");
    Say("Host:");
    Say(Machine.get_Host());

    // --- get_User ---
    Say("--- get_User ---");
    Say("User:");
    Say(Machine.get_User());

    // --- get_Home ---
    Say("--- get_Home ---");
    Say("Home:");
    Say(Machine.get_Home());

    // --- get_Temp ---
    Say("--- get_Temp ---");
    Say("Temp:");
    Say(Machine.get_TempDir());

    Say("=== Machine Demo Complete ===");
}
